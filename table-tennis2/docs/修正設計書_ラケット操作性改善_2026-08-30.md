# 修正設計書：ラケット操作性改善

本書は、公開済み `table-tennis2` v0.2.2 の `direct-paddle-v1` について、位置合わせ中の姿勢変化、pointer release後の短い接触猶予、補助輪郭端での極端な低品質化、最小viewportにおける縦方向の到達不能、迎球位置の分かりにくさを修正する v0.2.3 の実装正本である。入力資料 `修正設計書_ラケット操作性改善_2026-08-29.md` と外部レビュー `設計レビュー報告書_ラケット操作性改善_v0.2.3_2026-08-30.docx` は設計根拠として扱い、本書で現行コード・テスト・投影計測と照合して確定した仕様だけを実装対象とする。

- 作成日: 2026-08-30
- 対象アプリ: table-tennis2
- 種別: 修正設計書
- 対象バージョン: v0.2.3
- 前提コミット: `be6c24b3918f02289e6421d01b4106e50fdf1c39`（v0.2.2 公開・運用文書同期済み `master`）
- 対象ブランチ: `master`
- ステータス: 実装済み・本番完了（G1 / G2 / G3確認済み・正本）
- 現在フェーズ: Phase 10（締め済み、G1 / G2 / G3確認済み）
- 関連: [v0.2.1 ラケット追従性と返球操作の簡略化](./修正設計書_ラケット追従性と返球操作簡略化_2026-08-11.md) / [v0.2.2 コードレビュー指摘対応](./修正設計書_コードレビュー指摘対応_2026-08-16.md) / [v0.2.0 ダイレクトラケット操作と実衝突](./実装設計書_ダイレクトラケット操作と実衝突_2026-08-05.md) / [実装ログ](./実装ログ.md) / [設計書インデックス](./設計書インデックス.md) / [README](../README.md)

本書は、v0.2.1設計の §5 `RELEASE_GRACE_SEC`・`CONTACT_ASSIST_*`、§7 release期限、§8.1補助倍率、§8.3「contactQuality下限なし」、§9.2 release snapshot、§14の対応境界試験、§16の関連リスクを、本書の定数・状態・品質・検証契約で一部supersedeする。それ以外の最大16ms予測、pointer offset、interaction pose共有、depth gate、relative swept ellipse、1 flight 1 contact、`ShotIntent`分類、player専用solver、乱数3回の順序、legacy fallbackは維持する。

## 1. 目的・背景

### 1.1 目的

1. ボールへ位置を合わせるpassive trackingでは、ラケット面をニュートラル姿勢へ安定させる。
2. touchはrelease後350ms、mouse / penは220msまで、最後の位置で実衝突を受けられるようにする。
3. active strikeの鮮度は160msのまま維持し、拡大したcontact猶予へ古い技判定を持ち込まない。
4. pointer release後のcontact猶予は最初に成立したcontactで消費し、同じrelease操作を後続flightへ再利用させない。
5. 可視補助輪郭へball外周が届いた接触は、少なくとも通常のpassive PUSHを成立させ得る品質へ揃える。
6. 568×320を含む対応viewportで、通常のAI返球軌道が縦方向のclampだけを理由に到達不能にならないようにする。
7. 自動吸着を追加せず、実軌道から求めた迎球位置を弱いガイドとして表示する。

### 1.2 安定性優先順位

本修正では次の順で判断する。

1. 描画と衝突の同一pose、depth gate、1 flight 1 contactを壊さない。
2. passive操作とactive strikeを時間・姿勢の両方で分離する。
3. 補助輪郭の見た目と返球可能性を一致させる。
4. 既存物理・AI・サーブ・保存・PWAを変更しない。
5. 操作感の最終判定は自動試験で代替せず、iPhone Safari実機で確認する。

## 2. スコープ

### 2.1 IN

- `direct-paddle-v1` のpassive / active姿勢分離と角度補間のwrap対策。
- strike snapshotの160ms鮮度と、pointer種別別contact猶予の分離。
- touch assist `1.40`、mouse / pen assist `1.20`。
- 有効接触の `contactQuality` 下限 `0.40`。
- `PADDLE_SCREEN_Y_MIN` を `0.35` へ変更し、投影・到達可能性を固定母集団で検査する。
- 実際のplayer側bounce後に一度だけ計算する迎球位置ガイド。
- `?debugInput=1` のDOM観測値拡充。
- 対象unit、固定母集団校正、3環境E2E、実機受入。
- 実装時のv0.2.3版数、Service Worker cache、README、設計・Phase文書同期。

### 2.2 OUT

- ラケット中心のballへの自動移動、吸着、soft Y assist。
- pointer X/Yを打球方向へ変換する仕様変更。
- `direct-paddle-v2`、操作モード設定、難易度別 `ControlAssistProfile`。
- `InputController` のserve用 `Flick`、coalesced events、clock正規化の変更。
- `ShotIntent` の7技分類、power、aim、depth、spin式の変更。
- `solveDirectPlayerShot()`、`SHOTS`、`ensureNetClear()`、品質二乗誤差、乱数消費順の変更。
- AI判断、AI接触、サーブ、得点、11点2点差、戦績、IndexedDB schemaの変更。
- viewport gate、HUD、right rail、44px操作領域、2px下端余白の変更。
- 依存追加、API、外部送信、analytics、入力軌跡の永続化。
- commit、push、workflow dispatch、GitHub Pages公開。別途ユーザーが明示した場合だけ行う。

## 3. 現状コードと原因

### 3.1 処理経路

```text
PointerEvent
  -> src/input.ts InputController.ingest()/onPointerUp()
  -> src/control/stroke.ts deriveStrikeMetrics()
  -> src/control/paddle.ts PaddleController
  -> src/control/contact.ts sweptPaddleContact()
  -> src/control/shot-intent.ts buildShotIntent()
  -> src/physics.ts solveDirectPlayerShot()
  -> src/game.ts Game.directPlayerContact()
  -> src/render.ts Renderer
```

描画中心と衝突中心は `PaddlePose.screenX/screenY` を共有している。この所有権は変更しない。

### 3.2 原因A：passive移動が姿勢へ流入する

`PaddleController.applyInput()` はtracking速度 `0.15` 以上で `targetAngle` / `targetTilt` を更新する。一方、active strikeは `speed >= 1.10`、`displacement >= 0.04`、`verticality >= 0.55` のAND条件である。このため、技にならない位置合わせでもラケット面だけが回転する。

さらに現在の角度補間は `target - current` の単純差であり、`+π/-π` 境界を跨ぐtarget変更で遠回りする余地がある。

pointer release後の `follow` 中は `targetAngle` / `targetTilt` を更新しないため、ShotIntentが160ms超でpassive PUSHへ落ちても、直前のactive姿勢が残り得る。姿勢は描画だけでなく `sweptPaddleContact()` の回転楕円とtilt squashにも使われるため、strike鮮度と同時にframe単位でneutralへ戻す必要がある。

### 3.3 原因B：1つのrelease期限が3責務を持つ

`RELEASE_GRACE_SEC = 0.16` は次の用途で共有される。

- release後の `follow` phaseとcontact eligibility。
- release時に固定した `releaseStrike` snapshotの有効期間。
- `beginContact()` 後の `followUntil` 再設定。

単に値を350msへ伸ばすと、release時に `active: true` だった固定snapshotも再評価されず350ms残る。また `beginContact()` が期限を延長すると、接触後の新しいflightまで無入力でcontact eligibleになる危険がある。

期限の「非延長」だけでも不十分である。release後にcontactが成立しても元の350ms / 220ms期限が残ると、`flightId` 更新後の別incoming flightへ同じrelease操作を再利用できる。pointer down中の置きっぱなし接触は維持し、pointer up後の猶予だけを最初のcontactで消費する。

### 3.4 原因C：可視輪郭端の品質が0まで下がる

`sweptPaddleContact()` はassist楕円とball半径で接触を成立させるが、品質はball半径を含まないassist楕円に対して計算する。

```text
screenQuality = clamp(1 - sqrt(qAssist), 0, 1)
timingQuality = clamp(1 - depthDistance / tolerance, 0, 1)
rawQuality = 0.70 * screenQuality + 0.30 * timingQuality
```

輪郭端では `screenQuality = 0` になり得る。`solveDirectPlayerShot()` は `qualityLoss^2 * 0.16` を誤差へ加えるため、見える輪郭へ触れたのにネット・アウトになる割合が高い。また現在は `contactQuality > 0.3` のときだけ `ensureNetClear()` を通るため、極端な低品質はこの保護も受けない。

### 3.5 原因D：568×320でY上限側に届かない軌道がある

現在の `PADDLE_SCREEN_Y_MIN = 0.45` は、568×320で中心を144pxより上へ置けない。2026-08-30の設計調査では、現行の `OpponentAi.decideShot()` → `solveShot()` → `solveContactPlane()` → `predictAt()` → `projectWorldPoint()` を通す固定母集団30,362件について、Yだけが原因の到達不能がfine 6件、touch 3件あった。

同じ母集団で `PADDLE_SCREEN_Y_MIN = 0.35`、fine assist `1.20`、touch assist `1.40`、最大tilt時の縦squash `0.85` を適用すると、5 viewportすべてで到達不能は0件、最小余裕は568×320 fineの1.304pxだった。この結果から0.33への追加拡大は不要と判断し、変更量の小さい0.35を採用する。

### 3.6 原因E：迎球位置を示す状態がない

`Game` はAI打球時に `player.z = solveContactPlane(...)` を設定し、`predictAt()` と共有投影も利用できる。しかし現在の `RenderScene` は着地点 `mark` とsmash可否だけを持ち、player側bounce後の迎球位置を保持しない。

また、ガイドalphaはsimulation clockから求めるが、現行 `RenderScene` は `simulationTime` を持たない。Rendererがwall clockを独自参照したりGameのprivate状態を再取得したりしないよう、`simulationTime` とguideを同じscene snapshotで渡す必要がある。

## 4. 確定定数

`src/config.ts` で次を正本値とする。

| 定数 | v0.2.2 | v0.2.3 | 用途 |
|---|---:|---:|---|
| `STRIKE_ACTIVE_MAX_AGE_SEC` | `RELEASE_GRACE_SEC`と共用 | `0.16` | release snapshotをactiveとして使える最大age |
| `CONTACT_RELEASE_GRACE_TOUCH_SEC` | `0.16` | `0.35` | touch release後の位置保持・contact eligibility |
| `CONTACT_RELEASE_GRACE_FINE_SEC` | `0.16` | `0.22` | mouse / pen release後の位置保持・contact eligibility |
| `CONTACT_ASSIST_TOUCH` | `1.30` | `1.40` | 可視bladeに対するtouch assist倍率 |
| `CONTACT_ASSIST_FINE` | `1.15` | `1.20` | mouse / pen assist倍率 |
| `ASSIST_CONTACT_QUALITY_FLOOR` | なし | `0.40` | depthとscreen hitが成立した接触の品質下限 |
| `PADDLE_SCREEN_Y_MIN` | `0.45` | `0.35` | pointerから求めるbase centerの上側clamp。最大5%のpredictionは一時越境可 |
| `PADDLE_SCREEN_Y_MAX` | `0.90` | `0.90` | 下側clamp。変更しない |

`RELEASE_GRACE_SEC` は削除し、用途ごとの定数へ全参照を移す。`PADDLE_VISUAL_HOLD_*` のような同値の重複定数は追加せず、release後の表示保持はcontact graceと同じ `follow` 期限を使う。

## 5. 詳細仕様

### 5.1 passive / active姿勢

`PaddleController` は各input/frameでstrike状態を一度だけ求め、その結果をphaseと姿勢targetの両方へ使う。pointer down中は `deriveStrikeMetrics()`、release後の `follow` 中は経過ageを加えた `getStrikeMetrics(frameTime)` を正本とし、`advanceFrame()` でも必ずtargetを更新する。

| pointer / strike | phase | target angle | target tilt |
|---|---|---:|---:|
| down / `active = false` | `tracking` | `π / 2` | `0` |
| down / `active = true` | `armed` | `atan2(vy, vx) + π / 2` | `clamp(directionY + curvature * 0.4, -1, 1)` |
| up・grace内 / `active = true` | `follow` | `atan2(vy, vx) + π / 2` | `clamp(directionY + curvature * 0.4, -1, 1)` |
| up・grace内 / `active = false` | `follow` | `π / 2` | `0` |

- tracking速度 `0.15` による姿勢更新は削除する。
- pointerが動き続けていてもactive条件を満たさなければ、targetは毎回ニュートラルへ戻す。
- pointer入力が止まり80ms窓からactive sampleが外れた場合も、次frameでニュートラルtargetへ戻す。
- release後0〜160msはactive snapshot由来の姿勢を保持してよい。160msを超えた最初のframeではcontact eligibleが残っていても `targetAngle = π / 2`、`targetTilt = 0` へ切り替え、補間でニュートラルへ収束させる。
- `follow` 中の描画poseと `sweptPaddleContact()` が参照するposeは同一であり、161ms以後に古いDRIVE / CHOP姿勢を衝突楕円へ残さない。
- 位置、予測、速度、contact判定は従来どおりで、姿勢だけをstrike由来に限定する。

角度補間は次の最短差を使う。

```text
delta = atan2(sin(target - current), cos(target - current))
angle = current + delta * alpha
```

`shortestAngleDelta(current, target)` は `src/control/paddle.ts` のexportされた純粋関数とし、`tests/paddle-pose.test.ts` から直接境界検査する。補間tauとspeed ratioは現行値を維持する。

### 5.2 strike鮮度とcontact猶予の分離

#### pointer down中

- `deriveStrikeMetrics()` は既存80ms窓とactive 3条件を維持する。
- activeのage条件だけ `STRIKE_ACTIVE_MAX_AGE_SEC` を参照する。
- `tracking` / `armed` はcontact eligibleである。

#### release時

`PaddleController.release(time)` は次を順に行う。

1. 現在historyから `releaseStrike` を1回計算する。
2. `releaseStrikeCapturedAt = time` を保存する。
3. `pointerDown = false`、phaseを `follow` にする。
4. 保持済みpointerTypeがtouchなら `contactFollowUntil = time + 0.35`、mouse / penなら `time + 0.22` とする。
5. pointerTypeがnullなら `contactFollowUntil = time` とし、接触可能状態を新規に作らない。

#### release後のstrike再評価

`getStrikeMetrics(time)` は固定snapshotの数値をそのまま延命しない。

```text
releasedAge = releaseStrike.age + max(0, time - releaseStrikeCapturedAt)
active = releaseStrike.active && releasedAge <= 0.16
```

- contact grace内でも161ms以後は `active = false` とし、同じsnapshotの速度・方向はdebug用に保持してよいが、`buildShotIntent()` はpassive PUSHとして扱う。
- 160msちょうどはactive可、160msを超えた最初の時刻でfalseとする。
- contact grace期限後はzero metricsを返す。

#### contact時

`beginContact(time)` は `phase = "contact"` と `contactFlash = 1` を設定し、pointer状態により期限を次のように扱う。

- `pointerDown = true`: `contactFollowUntil` を変更しない。次frameのstrike評価で `tracking` / `armed` へ戻り、置きっぱなしのラケットが後続flightも返せる既存挙動を維持する。
- `pointerDown = false`: `contactFollowUntil = min(contactFollowUntil, time)` とし、release後の猶予をこのcontactで消費する。`isContactEligible()` は `contact` phaseを許可せず、次frameは期限超過として `recover` へ進むため、更新後の `flightId` に同じrelease操作を再利用できない。

`directPlayerContact()` は `sweptPaddleContact()` 成立と `lastPlayerContactFlightId` 更新の直後、solver結果の成否にかかわらず `beginContact(time)` を1回呼ぶ。solver非有限でも同じscreen contactを再評価せず、release猶予を残さない。contact時に期限を未来へ延長する処理は追加しない。

#### reset / cancel / suspension

`reset()` はsnapshot、captured time、contact期限、pointerType、debug値を全て破棄する。既存 `InputController.onPointerCancel()`、pause、portrait、too-small、pagehideからのreset経路を維持し、graceを再生成しない。

### 5.3 contact assistとquality floor

楕円の中心、角度、tilt squash、ball半径、relative sweep、depth toleranceは変更しない。

```text
assistVisualRx = visualRx * pointerAssistScale
assistVisualRy = visualRy * pointerAssistScale
collisionRx = assistVisualRx + ballRadius
collisionRy = assistVisualRy + ballRadius
```

接触が成立した後だけ品質下限を適用する。

```text
screenQuality = clamp(1 - sqrt(qAssist), 0, 1)
timingQuality = clamp(1 - depthDistance / tolerance, 0, 1)
rawContactQuality = clamp(0.70 * screenQuality + 0.30 * timingQuality, 0, 1)
contactQuality = max(0.40, rawContactQuality)
```

- assist外、depth外、pointerType未確定は従来どおり `null` であり、floorでhitboxを広げない。
- `screenQuality` と `timingQuality` はfloor前のraw値を `ContactEvent` へ残す。
- `contactOffsetX/Y` は従来どおりassist楕円基準であり、aimを変更しない。
- raw品質が0.40以上の中央・通常接触は値を変更しない。
- `solveDirectPlayerShot()` は変更しない。結果としてすべての有効接触が既存 `contactQuality > 0.3` 条件を満たすため、非arc shotは `ensureNetClear()` の対象になる。この分布変更を固定seed校正で検査する。

### 5.4 Y可動域

`PaddleController.baseCenter()` のclamp順序を維持し、上側比率だけ0.35へ変更する。

```text
rawY = stageY * height - pointerOffset * height
ratioClampedY = clamp(rawY, 0.35 * height, 0.90 * height)
screenY = clampPaddleScreenY(ratioClampedY, height, radius)
```

- `clampPaddleScreenY()` によるblade・handle下端保護を維持する。
- `PADDLE_SCREEN_Y_MIN = 0.35` は `baseCenter()` のpointer base centerにだけ適用する。`advanceFrame()` の最大16ms・画面高5% predictionは追従性維持のため上側境界を一時的に越えてよく、prediction後に0.35へ再clampしない。
- 最大上方向prediction時の理論下限は `0.30 * height` 付近だが、最終poseは従来どおりblade / handleの描画境界を守る `clampPaddleScreenY()` の制約を受ける。
- 画面上端0まで広げない。
- HUD、Canvas寸法、pointer offset、X clamp、`PADDLE_LIMIT` は変更しない。
- Y到達試験では、Xが `PADDLE_LIMIT` 内の有効返球だけを母集団とし、X到達性の再設計と混ぜない。

### 5.5 迎球位置ガイド

#### 状態所有権

`src/types.ts` に次の読み取り用状態を追加する。

```text
PlayerContactGuide {
  x: world X
  y: world Y
  z: player contact plane
  plannedAt: simulation time
  etaSec: bounce時点からcontact planeまでの予測秒
}
```

`Game` が `playerContactGuide: PlayerContactGuide | null` を所有する。Rendererは再計算せず描画だけを行う。

`RenderScene` は少なくとも次の2項目を同じframe snapshotとして公開し、`Game.getRenderScene()` がprivate状態から設定する。

```text
simulationTime: number
playerContactGuide: PlayerContactGuide | null
```

Rendererは `RenderScene.simulationTime` だけをalpha計算に使い、`performance.now()`、`Date.now()`、Game private fieldを参照しない。

#### 計算タイミング

- `updatePhysics()` はtable bounceごとに `judgeBounce()` を完了した直後、`planPlayerContactGuideIfNeeded()` を1回呼ぶ。helperは次をすべて満たすときだけ計算し、通常AI返球とAIサーブ2nd bounceを同じ契約で扱う。

```text
state.phase === "rally"
ball.live === true
ball.hitter === "A"
ball.bounces === 1
ball.z < 0
ball.vz < 0
playerContactGuide === null
```

- point確定、serve 1st bounce、AI側bounce、player起点flight、2nd rally bounceではhelperが何もしない。
- `predictAt(this.ball, this.player.z, -1, FLOOR)` を使い、成功時にworld座標と `etaSec` を保存する。
- per-frameで `predictAt()` を呼ばない。軌道シミュレーションはincoming flightにつき1回とする。
- net、table外、予測失敗ではガイドを作らない。

#### 破棄タイミング

- playerのscreen contactが成立した時点。solverが非有限で返球できない場合も再接触させない現行flight lockに合わせて破棄する。
- point、new game、serve開始、player起点の新flight。
- `ball.hitter !== "A"` またはphaseがrally以外へ変わった場合は描画しない。状態が残っていても表示条件でfail closedにする。
- pointer cancel / pause / pagehideではball flightとsimulationが維持されるためguide stateを破棄しない。入力期限だけをresetし、復帰後は同じworld予測を現在cameraで再投影する。

#### 表示条件

次をすべて満たす場合だけ表示する。

```text
controlModel === "direct-paddle-v1"
state.phase === "rally"
ball.live === true
ball.hitter === "A"
ball.bounces >= 1
ball.vz < 0
playerContactGuide !== null
```

#### 描画

- `Renderer.render()` は `drawMark()` の後、`drawBall()` の前に `drawPlayerContactGuide()` を呼ぶ。
- world座標をRendererの現在cameraで毎frame投影し、viewport変更へ追従させる。
- 半径は `clamp(paddleScreenRadius(width, height, paddleDepthRatio(z)) * 0.55, 7, 12)` px。
- stroke色は `rgba(126, 224, 168, alpha)`、塗りなし、line widthは `max(1.5, radius * 0.16)` px。
- 残り時間 `remaining = etaSec - (simulationTime - plannedAt)` によりalphaを決める。

| remaining | alpha |
|---|---:|
| `> 0.35s` | `0.25` |
| `0.10s < remaining <= 0.35s` | `0.40` |
| `0s < remaining <= 0.10s` | `0.40 * remaining / 0.10` |
| `<= 0s` | 非表示 |

ガイドは「現在軌道から求めた迎球目安」であり、ラケット・ball・衝突状態へ副作用を持たない。

### 5.6 debug観測

`?debugInput=1` のときだけ、既存datasetに次を追加・同期する。

| dataset | 値 | stale防止 |
|---|---|---|
| `data-paddle-angle` | 現在angle、rad、3桁 | poseなしで削除 |
| `data-paddle-tilt` | 現在tilt、3桁 | poseなしで削除 |
| `data-contact-grace-ms` | release後の残りcontact grace、整数ms | tracking/armed/recover/idleで`0` |
| `data-screen-quality` | 最後に成立したcontactのraw値、3桁 | new flight / reset / pointで削除 |
| `data-timing-quality` | 同上 | 同上 |
| `data-contact-quality` | floor適用後、3桁 | 同上。既存属性を継続 |
| `data-predicted-contact-x` | ガイド投影X、2桁 | ガイド非表示・破棄で削除 |
| `data-predicted-contact-y` | ガイド投影Y、2桁 | 同上 |
| `data-predicted-contact-remaining-ms` | 予測残り時間、0以上の整数 | 同上 |

既存 `data-paddle-phase`、`data-strike-active`、`data-strike-speed`、`data-contact-assist-scale` は維持する。debug値、座標、軌跡を外部送信・保存しない。

## 6. 状態遷移

```text
idle
  -> pointerdown -> tracking
tracking
  -> active strike -> armed
armed
  -> active失効 -> tracking
tracking / armed
  -> pointerup -> follow (pointer別contact期限、release snapshot保存)
tracking / armed / follow
  -> screen+depth contact -> contact (flash、1 flight lock。pointer up後だけrelease期限を現在時刻へ短縮)
contact
  -> next frame -> tracking / armed (pointer down中)
  -> next frame -> recover (pointer up後。release猶予は消費済み)
follow
  -> contact期限超過 -> recover (snapshot破棄、pointerTypeはrecover完了まで保持)
recover
  -> 待機位置到達 -> idle (pointerTypeをnull化)
any
  -> cancel / suspension / point reset -> recoverまたはidle、全期限破棄
```

時間比較は既存wall clock `performance.now()/1000` domainを使い、`simulationTime` はガイドの残り表示だけに使う。contact eligibilityへsimulation clockを混ぜない。

## 7. 既存機能の非破壊条件

### 7.1 direct操作

- pointer X/Y、touch 6% / mouse・pen 1% offset、16ms / 高さ5%予測を維持する。
- interaction / render / contact centerを同一値に保つ。
- relative swept ellipse、ball外周、depth gate、pointerType必須を維持する。
- `lastPlayerContactFlightId` による1 flight 1 contactを維持する。
- pointer up後に成立したcontactはrelease猶予を1回で消費し、pointer down中のcontactは後続flightへのeligibilityを維持する。
- `aimX = clamp(-0.65 * contactOffsetX, -0.65, 0.65)` を維持する。
- passiveはPUSH、activeだけが技・power・spinを持つ。

### 7.2 物理・AI・ゲーム

- `FIXED_STEP = 1 / 240`、`MAX_SUBSTEPS`、integrate、bounce、netを変更しない。
- `solveDirectPlayerShot()` のtarget、speed model、spin carry、quality二乗誤差、乱数3回の順序を変更しない。
- AIの判断、接触面、乱数順、serve種類・長さを変更しない。
- 11点2点差、serve / rally / point、score、戦績を変更しない。

### 7.3 保存・PWA・プライバシー

- IndexedDB名 `table-tennis2`、schema、既存データを変更しない。
- 公開URL、manifest identity、root/child Service Worker分離を変更しない。
- 実装時は `package.json` / `package-lock.json` をv0.2.3へ同期し、child cacheを `table-tennis2-v0.2.3` にする。
- 新規依存、権限、network request、telemetryを追加しない。

## 8. 変更ファイルとsymbol

| ファイル | 実装契約 |
|---|---|
| `src/config.ts` | 共用release定数を用途別定数へ置換、assist、quality floor、Y minを確定値へ更新 |
| `src/control/stroke.ts` | active age参照を `STRIKE_ACTIVE_MAX_AGE_SEC` へ変更。80ms窓と3条件は維持 |
| `src/control/paddle.ts` | frameごとのstrike由来姿勢、`shortestAngleDelta()`、release snapshot経過age、pointer別期限、pointer up contact時のgrace消費、debug grace |
| `src/control/contact.ts` | assist倍率更新、raw品質を保持したfloor適用 |
| `src/types.ts` | `PlayerContactGuide`、`RenderScene.simulationTime`、`RenderScene.playerContactGuide` を追加。保存schemaは変更しない |
| `src/game.ts` | `planPlayerContactGuideIfNeeded()`、guide破棄、RenderScene/debug同期、screen contact直後の `beginContact()`、contact raw値観測 |
| `src/render.ts` | sceneのsimulationTimeでballより後ろへguide描画。既存paddle / assist描画は同じscaleを利用 |
| `tests/paddle-pose.test.ts` | 姿勢、wrap、follow 160/161ms姿勢、touch/fine期限、release contactでのgrace消費、pointer down維持、reset境界 |
| `tests/paddle-contact.test.ts` | 1.40/1.20境界、quality floor、assist/depth外非接触 |
| `tests/stroke-math.test.ts` | 新active age定数と既存80ms・3条件回帰 |
| `tests/direct-calibration.test.ts` | PUSH quality 0.40固定母集団、contact→intent→solver製品経路、既存high/low校正 |
| `tests/reachable-contact.test.ts` | AI製品経路・投影・Y到達性の固定母集団 |
| `tests/game-smoke.spec.js` | release後passive接触、guide条件・寿命・debug、3環境製品経路 |
| `package.json` | `test:unit`へ新規testを登録。実装完了時にv0.2.3へ更新 |
| `package-lock.json` / `sw.js` | 実装完了時の版数・cache同期。依存版は変更しない |
| `README.md` | 操作、release猶予、assist、迎球ガイド、版数を実装結果へ同期 |
| `docs/実装ログ.md` | Phase 10の着手・各gate・G2・公開結果を実装時に記録 |
| 本書 / `docs/設計書インデックス.md` | 実装状態、反映commit、残課題を同期 |

`src/input.ts`、`src/control/shot-intent.ts`、`src/physics.ts` は変更不要を基本とする。実装時に変更が必要と判明した場合は、先に本書のスコープ・非破壊条件・テストを更新する。

## 9. 実装順序とPhase gate

### Phase 10-0：着手と負の証拠

- Git root、branch、HEAD、upstream、作業ツリーを再確認する。
- `docs/実装ログ.md` にPhase 10を開始し、本書を正本として登録する。
- 現行定数のtargeted unit 30/30を実行する。
- 568×320の固定母集団を旧Y min 0.45で実行し、fine 6件 / touch 3件の到達不能を負の対照として記録する。母集団や件数が変わる場合は原因を調べ、本書へ実測を追記してから進む。

### Phase 10-1：姿勢分離

- `deriveStrikeMetrics()` の結果をphase・targetへ一元利用する。
- passiveのニュートラルtargetと最短角度差を実装する。
- pose unitを通し、位置・予測・contact centerに差分がないことを確認する。

### Phase 10-2：時間責務分離

- 共用release定数を削除し、strike ageとpointer別contact期限を実装する。
- frozen snapshotの経過age、160/161ms、220/221ms、350/351msをunitで固定する。
- follow中の姿勢targetもstrike ageへ連動させ、160msではactive姿勢、161msではcontact eligibleのままニュートラルtargetとなることを確認する。
- `beginContact()` がpointer up後の期限をcontact時刻へ短縮し、pointer down中はeligibilityを維持すること、cancel/resetで0になることを確認する。

### Phase 10-3：assistとquality

- assist倍率とquality floorを実装する。
- collision境界、floor、assist外、depth外、pointerType nullをunitで固定する。
- q=0.40校正と既存high/low校正を通す。

### Phase 10-4：Y到達性とguide

- Y min 0.35と `reachable-contact.test.ts` を実装する。
- `planPlayerContactGuideIfNeeded()` を `judgeBounce()` 完了直後へ接続し、通常AI返球とAIサーブを同じ条件でbounce後一回だけ計算する。
- `RenderScene` へ `simulationTime` とguideを同時に渡し、状態・描画・debugを接続する。
- per-frame予測を追加していないこと、stale datasetが残らないことをunit/E2Eで確認する。

### Phase 10-5：G1・文書・版数

- v0.2.3版数/cache、README、本書、索引、実装ログを同期する。
- `npm run check` と安定性反復、既存root版、合成Pages成果物検査を完了する。
- validator、link、`git diff --check`、task対象差分を確認する。

### Phase 10-6：G2実機受入

- Google Chromeと最低1台のiPhone Safariで §11.2 を実施する。
- 自動E2Eを実機操作感の代替にしない。未実施ならPhase状態に `G2未了` を残し、本番完了としない。

### Phase 10-7：G3公開

- commit / push / GitHub Pages公開の明示指示がある場合だけ実施する。
- root + child合成artifact、Actions、deployment SHA、HTTP/PWA、公開Chromeを確認する。

## 10. テスト設計

### 10.1 pose / phase / time

`tests/paddle-pose.test.ts` に次を追加する。

- speed 0.80のpassive trackingはtarget angle `π/2`、tilt `0`へ収束する。
- active upward / downwardだけが姿勢targetを変える。
- current `π - ε`、target `-π + ε` と逆方向でdelta絶対値が `2ε` 以下になる。
- touchは350msちょうどまでeligible、350ms超でfalse。
- mouse / penは220msちょうどまでeligible、220ms超でfalse。
- release snapshotは160msちょうどまでactive、160ms超でpassive。同時にtouch contactは350msまでeligibleである。
- active release後160msではactive姿勢target、161msではcontact eligibleのままtarget angle `π/2`、target tilt `0`となる。
- release後100msで `beginContact()` すると即contact不可となり、新しいflight相当の再判定でも元の350ms以内にeligibleへ戻らない。
- pointer down中の `beginContact()` は次frameでtracking / armedへ戻り、後続flight相当でもeligibleを維持する。
- pointerType nullのreleaseは接触を作らない。
- cancel/reset/pause相当reset後は期限、snapshot、assistが残らない。

### 10.2 contact

`tests/paddle-contact.test.ts` で次を固定する。

- touch `1.40`、mouse / pen `1.20` の輪郭内、一致、ball外周一致、直後。
- assist端・timing端でも、contactが成立した場合は `contactQuality === 0.40` 以上。
- 中央のraw品質0.40超はfloorで変わらない。
- `screenQuality` / `timingQuality` はfloorされない。
- assist外、depth外、pointerType nullは `null`。
- tilt `0` / `±1`、depth ratio `0 / 0.5 / 1` で有限値を保つ。

### 10.3 物理校正

既存seed `20260805`、3打点、各400試行を維持する。`buildShotIntent()` → `solveDirectPlayerShot()` の製品intent経路でpassive PUSH、quality `0.40`を追加する。

```text
total = 1,200
台内率 >= 0.70
```

2026-08-30の現行solver実測は849/1,200、70.75%である。これは採用前実測であり、実装時は同じseed・順序で再確認する。既存quality 0.85 / 0.25のhigh率とmiss gap、side spin符号反転も維持し、solver自体の変更を検出する。

さらに1〜3ケースの製品経路integration testを追加する。assist端、timing中央、passive strikeを `sweptPaddleContact()` へ通し、`contactQuality === 0.40`、`buildShotIntent()` の `classifiedShot === "PUSH"` とpassive、`solveDirectPlayerShot()` の有限解を同じケースで確認する。contact・intent・solverを個別試験だけで代替しない。

### 10.4 Y到達固定母集団

`tests/reachable-contact.test.ts` は既存製品関数を使い、独自の簡略投影を作らない。

#### 母集団

- 乱数: `tests/direct-calibration.test.ts` と同じLCG、seed 1〜30。各候補でseedを初期化する。
- 難易度: `easy / mid / hard`。
- AI接触入力: `y = [-42, -12.001, 0, 14, 22, 24, 52, 105]`、`z = [30, 100, 178]`、`x = [-30, 0, 30]`。
- bounce: `lastBounceZ = [30, 70]`、player X `[-104, 0, 104]`。
- ball基準: `vx=0`、`vy=-100`、`vz=400`、`side=0`、`spin`は `y < 14 ? -0.5 : 0.5`。
- 経路: `OpponentAi.decideShot()` → `solveShot()` → `simLand()` → `solveContactPlane()` → `predictAt()` → `projectWorldPoint()`。
- 採用候補: player側tableへnetなしで着地し、predictionが非null、`abs(predicted.x) <= PADDLE_LIMIT`。

前提コードでは採用候補30,362件、全7 shot、全3難易度を含む。件数・種類が変わった場合はAI/物理への意図しない差分として調査する。

#### viewportと判定

```text
568x320
667x375
760x360
844x390
900x412
```

各候補・viewport・pointer種別について、最大tiltの `squash = 0.85` を使う。

```text
centerMin = 0.35 * height
centerMax = min(0.90 * height, height - paddleBottomExtent(radius))
dy = projectedYから[centerMin, centerMax]までの距離
reachableRy = visualRy * 0.85 * assistScale + ballRadius
margin = reachableRy - dy
```

- fine `1.20`、touch `1.40` の双方で到達不能0件。
- 全marginが1.0px以上。
- 568×320を最小marginケースとして含む。
- base centerが0.35にclampされた状態で最大上方向predictionを与え、比率0.35への再clampがなく、最終poseが `clampPaddleScreenY()` の描画境界内かつ有限であることをunit/E2Eで確認する。

### 10.5 guide unit / E2E

- player側の実bounce前は非表示、bounce確定後だけ表示する。
- `planPlayerContactGuideIfNeeded()` は通常AI返球とAIサーブ2nd bounceで各flight 1回だけ生成し、serve 1st bounce、player shot、AI側bounce、point確定では生成しない。
- `predictAt()` null、net、table外、player shot、point、resetで非表示・状態破棄。
- `Game.getRenderScene()` の `simulationTime` がGameのsimulation clockと一致し、Rendererのremaining/alpha計算がwall clockへ依存しない。
- viewport resize後もworld座標から再投影し、予測world点は変えない。
- `remaining` の0.35 / 0.10 / 0秒境界でalphaを検査する。alpha計算は純粋helperへ分離してよい。
- `?debugInput=1` で予測X/Y/残り時間が有限、guide破棄後に属性が無い。
- touchをreleaseし200〜300ms後に接触させるとcontactし、161ms超のためpassive PUSHになる。
- touch 450ms後、fine 320ms後はreleaseだけではcontactしない。厳密な1ms境界はunitが責任を持つ。
- active upward / downward、assist外miss、depth外miss、1 flight 1 contactを回帰する。
- Desktop Chromium、Android相当Chromium、iPhone相当WebKitで実行する。
- 短時間の瞬間 `data-strike-active` は既存方針どおりunitとChromiumが担当し、WebKitは最終接触・reset結果を担当する。

### 10.6 G1コマンド

```powershell
npm run lint
npm run typecheck
npm run check:app
npm run build
npm run check:dist
npm run test:unit
npm run test:e2e
npm run test:e2e:stability
npm run check
```

公開を伴う場合は同一Pages originの回帰として `D:\my-app2\sitar\table-tennis` の `npm run check` と、root + child合成artifactのbuild/checkも実施する。Windows sandboxの `spawn EPERM` はrunner起動失敗と製品failを分離し、同じrevision・コマンドを許可済み環境で再実行する。

## 11. 受け入れ条件

### 11.1 G1：自動検証

- [x] passive trackingでラケットがニュートラル姿勢へ戻る。
- [x] active strikeだけが方向由来の姿勢を持つ。
- [x] `+π/-π` 境界で長距離回転しない。
- [x] touch 350/351ms、mouse・pen 220/221ms境界が正しい。
- [x] strike snapshotは160/161ms境界が正しく、contact猶予と独立している。
- [x] follow中の姿勢は160msまでactiveを保持でき、161msではcontact eligibleのままニュートラルへ戻る。
- [x] pointer up後のcontactはrelease猶予を消費し、pointer down中のcontactだけが後続flightのeligibilityを維持する。
- [x] touch assist 1.40、fine assist 1.20と可視輪郭が一致する。
- [x] 成立したcontactはquality 0.40以上、assist/depth外はmiss。
- [x] PUSH q=0.40の固定母集団台内率が70%以上。
- [x] assist端のcontact→PUSH intent→solver製品経路がquality 0.40と有限解を保つ。
- [x] Y到達母集団30,362件×5 viewport×2 pointer種別で0 unreachable、margin 1px以上。
- [x] Y min 0.35はbase center clampとして働き、最大上方向predictionは0.35を再clampせず、最終poseは描画境界内に収まる。
- [x] guideは実bounce後だけ表示し、1 flight 1回だけ予測する。
- [x] 通常AI返球とAIサーブのguide生成条件がhelperへ集約され、sceneのsimulationTimeで寿命を描画する。
- [x] unit、3環境E2E、worker 1・retry 0・3反復が全件成功する。
- [x] 20分相当240Hz pose/physics有限値検査を維持する。
- [x] validator `valid: true`、errors 0、warnings 0、`git diff --check`成功。

### 11.2 G2：Google Chrome・iPhone Safari実機

最低1台のiPhone Safariを必須とし、横画面568×320以上で次を確認する。

1. 置くだけ返球: ガイドへ位置を合わせ、指を離し、200〜300ms後に到達するballを5回中4回以上PUSHで返せる。
2. 上フリック: DRIVEまたは条件成立時SMASH / FLICKになる。
3. 下フリック: CHOPまたは短球時STOPになる。
4. passive位置合わせ中にラケット面が大きく回転しない。
5. assist外へ明確に置くと空振りする。
6. ガイドはball軌道の目安として見え、ball・ラケット・HUDを隠さず、接触直前に消える。
7. 568×320で上側・下側の代表球へblade / handleを画面内に保って届く。
8. 1試合を完走し、score、serve、戦績、pause、音、振動に回帰がない。

Google Chrome 844×390 / 568×320でも開始、direct / legacy、guide、console warning/error 0件を確認する。Android Chrome実機は推奨だが、必須G2はiPhone Safariとする。未実施項目は `未実施` と記録し、自動試験で置き換えない。

### 11.3 G3：公開（明示指示時のみ）

- [x] 対象commit `0462fa509240b7ddec8b8403fbd2ba32f79d062e` のActions build/deployがsuccess（run `33297273820`）。
- [x] Pages deployment `6164029528` のSHAが対象commitと一致。
- [x] root / childのHTML、JS、CSS、manifest、Service Worker、icons計16資産がHTTP 200で、binaryはSHA-256一致、textはCR正規化後にlocal artifactと一致。
- [x] child cacheが `table-tennis2-v0.2.3`で、旧child cacheを使用しない。
- [x] 公開Google Chromeで844×390のdirect既定・試合開始・guide、568×320のlegacy query・試合開始、console error/warning 0件を確認。

## 12. エラー・中断・部分失敗

- 非有限strike / pose / guide predictionはzero metricsまたはguideなしへfail closedし、前回active・前回guideを延命しない。
- pointerType不明ではassistもrelease contactも無効にする。
- guide計算失敗はゲームを停止せず、ガイドだけ非表示にする。consoleへ座標や入力履歴を出さない。
- direct shot solverが非有限の場合は既存warnと失敗表示を維持し、自動補正・再乱数化しない。
- suspension / cancel / pagehideは全input期限を破棄して復帰時の自動contactを防ぐ。guideはball flightとともに保持し、表示条件外では描画しない。
- build/test runnerの起動失敗とtest assertion failを別に記録する。

## 13. リスクと対策

| リスク | 予防 | 検出 | 回復 |
|---|---|---|---|
| contact猶予延長で古い技が残る | snapshot ageをrelease後も加算し160msでfalse | 160/161msと200〜300ms passive E2E | strike定数とsnapshot評価だけをrevert |
| contact後に同じrelease操作が別flightへ再利用される | pointer up contactで期限を現在時刻へ短縮。pointer down時だけ維持 | release→contact→新flight相当unit/E2E、pointer down対照 | grace消費処理だけをrevertし、設計再検討 |
| strike失効後も古い姿勢が衝突へ残る | follow中もframeごとにstrike ageからtarget更新 | 160/161ms pose/contact unit | follow target更新だけをrevert |
| poseが角度境界で大回転する | 最短角度差 | ±π unit、G2 | neutral targetへ安全fallback |
| assistで空振りが消える | floorは成立済みcontactだけ。hitbox式は別 | assist直後・depth外miss | assist/floorを旧値へ戻す |
| quality floorでゲームが易しくなりすぎる | solver不変、q=0.40と既存high/lowを同時校正 | 固定seed統計、G2 1試合 | floorのみ0.40未満へ変更せず設計再検討 |
| Y拡大でHUDやhandleと重なる | min 0.35、bottom clamp維持、5 viewport | reach test、568×320 G2 | Y minを旧0.45へrevert |
| guideが誤軌道・stale表示になる | 実bounce後一回計算、flight lifecycleで破棄 | state unit、debug属性削除E2E | guideだけ無効化 |
| guide予測がframe負荷を増やす | per-flight一回、renderは投影だけ | call count unit、stability E2E | guideだけ無効化 |
| 自動試験を操作感合格と誤認する | G1/G2を分離 | 実装ログの未実施表示 | iPhone実機まで本番完了にしない |

## 14. 採用しない案

| 案 | 採用しない理由 |
|---|---|
| ラケットをball / guideへ自動吸着 | 手動X/Yと実衝突、明確な空振りを失う |
| `RELEASE_GRACE_SEC = 0.35` だけへ変更 | frozen active snapshotとcontact後再延長を同時に伸ばす |
| visual hold専用の同値期限を追加 | v0.2.3では表示保持とcontact保持が同じ要件で、重複状態になる |
| quality floorをsolver内で全shotへ適用 | AI、legacy、独立solver入力まで変わり、assist成立時だけという境界を失う |
| assist端だけを複雑な分岐でfloor | 初版の条件と検証軸を増やす。成立済みcontact全体へ単純適用してraw成分を保持する |
| Y minを0または0.33へ広げる | 0.35で固定母集団0 unreachable・1px余裕を満たし、追加変更の根拠がない |
| guideを毎frame再シミュレーション | 軌道はflight中に決定済みで、負荷と揺れだけを増やす |
| 難易度別assist profile | AI難易度と操作補助の再設計、UI・保存・試験軸が必要で本修正を超える |
| `direct-paddle-v2`併存 | 同じ経路を二重保守する。既存legacy queryとcode rollbackで回復可能 |

## 15. ロールアウト・ロールバック

### 15.1 ローカル実装

- Phase 10-1〜10-4を分割し、各gate成功後だけ次へ進む。
- G1後に版数・cache・README・正本・索引・実装ログを同期する。
- G2未了なら状態を明記し、本番完了としない。

### 15.2 即時回避

- `?controlModel=legacy` を非保存の緊急回避として維持する。
- guideは描画状態だけであり、問題時はguide生成・描画を無効化してdirect衝突を維持できる。

### 15.3 code rollback

重大な操作不能時はv0.2.2へ戻し、次の単位でrevertできる構成を保つ。

1. guide状態・描画・debug。
2. Y min、assist、quality floor。
3. contact grace / snapshot age。
4. passive / active姿勢。

公開rollback時もroot + child合成artifact、Actions、deployment SHA、HTTP/PWA、Chromeを再確認する。

## 16. 未確定・要確認事項

- 実装開始を妨げる未確定事項はない。
- 本書の数値は2026-08-30の現行HEADと固定母集団実測を初期正本とする。実装者が操作感だけで変更せず、境界試験またはG2不成立時は実測・変更値・影響testを本書の変更履歴へ先に追記する。
- q=0.40の70%は固定seed回帰閾値であり、すべての実プレイでの成功保証ではない。
- Y母集団はXが `PADDLE_LIMIT` 内の有効返球に対する縦到達性であり、全乱数列・全将来AI変更の数学的保証ではない。
- iPhone Safari実機の操作感、safe-area、5回中4回の置くだけ返球は設計時点で未実施であり、G2で確認する。

## 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-08-31 | ユーザー確認により、§11.2で必須とするiPhone Safariを含む実機G2受入は確認済みとなった。端末機種・OS版・個別試行ログは未提供のため、受入結果のみを記録し、v0.2.3を本番完了へ更新。 |
| 2026-08-30 | commit `0462fa509240b7ddec8b8403fbd2ba32f79d062e`をmasterへpush。Actions run `33297273820`、Pages deployment `6164029528`、HTTP/PWA `Ready: true`、公開16資産、公開Chrome direct / legacy・試合開始・guide・console 0件を確認し、Phase 10を締めた。iPhone Safari実機G2は未実施のため本番完了扱いにはしない。 |
| 2026-08-30 | Phase 10-0〜10-5を実装しG1完了。package / lock / child cache / READMEをv0.2.3へ同期し、unit 108/108、3環境E2E 90/90、反復270/270、PUSH q=0.40、AI製品経路30,362件、既存root worker 1 E2E 66/66、合成artifact、validatorに成功。iPhone Safari実機G2とG3公開確認は未実施。 |
| 2026-08-30 | ユーザーの実装・本番公開・文書更新・push指示によりPhase 10を開始し、状態を「実装中（正本）」へ更新。 |
| 2026-08-30 | 外部設計レビューを現行コードへ照合し、follow中の160/161ms姿勢遷移、pointer up contact時のrelease grace消費、`RenderScene.simulationTime`受渡し、`PADDLE_SCREEN_Y_MIN`のbase clamp境界、`planPlayerContactGuideIfNeeded()`、contact→intent→solver integration testを追記。定数値と実装スコープは変更なし。 |
| 2026-08-30 | 新規作成。添付案を現行HEAD `be6c24b`、targeted unit 30/30、投影・AI製品経路30,362件、PUSH q=0.40固定seed1,200件で照合し、v0.2.3のpassive姿勢、strike/contact期限分離、assist 1.40/1.20、quality floor 0.40、Y min 0.35、bounce後一回の迎球ガイド、Phase 10 gateを着手前正本として確定。コード・実装ログ・版数・公開物は未変更。 |
