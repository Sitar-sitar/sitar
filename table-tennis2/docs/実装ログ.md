# 実装ログ（table-tennis2）

`D:\my-app2\docs\実装フェーズ記録ルール.md` に従い、実装の過程をフェーズ単位で記録する。
次フェーズ着手前に現フェーズを「締めチェックリスト」で必ずクローズする。

- 対象アプリ: table-tennis2
- 現在フェーズ: Phase 7（v0.2.0 ダイレクトラケット操作と実衝突）
- 現在ステータス: 進行中

---

## 現在進行中フェーズ

### Phase 7: v0.2.0 ダイレクトラケット操作と実衝突

- 目的 / スコープ: player入力をpointer X/Y追従へ変更し、共有投影によるscreen-space swept ellipse + depth gate、1 flight 1 contact、連続ShotIntent、incoming spin carry、player専用solverを実装する。AI、選択式サーブ、保存、HUD、PWA identityは維持する。
- 対象バージョン: v0.2.0
- 関連設計書: [ダイレクトラケット操作と実衝突](./実装設計書_ダイレクトラケット操作と実衝突_2026-08-05.md)
- 着手日: 2026-08-05
- ステータス: 進行中

**作業メモ（随時追記）**

- 2026-08-05: `master` / `origin/master` / 前提commit `98fd43a`の一致、GitHub Pages `build_type=workflow`、静的配布適格性、root+child合成配布、危険なtracked pathなしを確認した。ユーザー指示により、ローカル実装・検証・文書同期・commit・master push・Pages公開・Chrome確認までをPhase 7の完了範囲とする。
- 2026-08-05: `stroke` / `paddle` / `contact` / `shot-intent` / `projection` を純粋moduleとして追加し、coalesced Pointer Event、X/Y姿勢、logical/render分離、swept ellipse + depth gate、flight lock、incoming spin carry、固定3乱数のplayer専用solverをGame/Rendererへ結線した。既定はdirect、`?controlModel=legacy`を非保存fallback、`?debugInput=1`を製品経路E2E観測用とした。
- 2026-08-05: 固定seed `20260805`・代表3条件・各quality 400試行の校正で、初期の線形品質誤差はCHOP/PUSHのミス率差10ポイントを満たさないことを確認した。正本と実装を `qualityLoss^2 * 0.160` へ同期し、DRIVE/CHOP/PUSHのhigh品質台内率85%以上、SMASH 70%以上、low/highミス率差10ポイント以上、横回転平均着地点の符号反転を製品solver経路で固定した。
- 2026-08-05: 単体77/77、direct実衝突・legacy fallback・pagehideを含む3環境E2E 78/78を確認した。700ms callback回数を数える既存pagehideテストは、高負荷時にキャンセル済みqueueや次ポイントまで数える代理指標だったため、実際のサーブ属性変化を監視する製品状態テストへ修正した。
- 2026-08-05: `npm run check`、worker 1・retry 0の安定性反復234/234、設計validator errors 0 / warnings 0、`git diff --check`を完了した。既存root版は通常checkを通過し、並列E2Eで既知の短球合成入力が2件だけ一過性に失敗したが、Pages CIと同じworker 1・retry 0の全66件再実行は成功した。
- 2026-08-05: Google Chromeで844×390 / 568×320のproduction buildを確認し、direct既定、X/Yラケット追従、サーブ開始、legacy query、console warning/error 0件を確認した。短い返球窓へ遠隔操作を合わせる実返球・空振り比較は未完了で、製品経路E2Eの実衝突成功をChrome手動passとは代替せず、公開後確認とユーザー実機確認の残項目として扱う。

---

## 完了フェーズ（締め済み）

### Phase 6: v0.1.1 横画面HUDコーナースコア化 — 完了

- 完了日: 2026-08-03
- 実装内容: 得点をstage左右上隅の小型バッジへ分離し、中央メタを縮小した。left railへ読み取り専用の現在サーバーFeatureModule、right railへ設定・音・振動・試合状態を追加し、既存ID、ゲーム物理、AI、入力座標、保存データ、公開URLを維持した。v0.1.1とchild Service Worker cacheを同期し、Pages成果物検査は各package版数からcache名を導出するよう恒久化した。
- 反映コミット: `fad632b16c84a4b2b385d5dbffe3f2984d393b3a`（HUD実装・設計・QA） / `d4c802b9f04116d6e36da74be8a1f76fe50fff16`（Pages cache版数検査）
- 検証結果: 負のコントロール、`npm run check`（単体62件・3環境E2E75件）、安定性反復225/225、既存版`npm run check`（単体56件・E2E66件）、設計validator（errors 0 / warnings 0）、合成Pages検査、`git diff --check`が成功。Chrome 844×390 / 568×320のローカルvisual QAはpassed。初回run `30810290992`で旧固定cache期待を検出後、修正版run `30810815952`はbuild/deploy success、Pages deployment `5725645750`はSHA `d4c802b`一致・status success。公開HTTP/PWAは`Ready: true`、Service Workerはv0.1.1を含みv0.1.0を含まない。公開Chromeで92×48 / 76×44の得点、132×28 / 112×24の中央メタ、開始・サーブ・設定pause、公開warning/error 0件を確認した。
- 公開先: `https://sitar-sitar.github.io/sitar/table-tennis2/`
- 残課題・既知の問題: Phase 6の新規残課題なし。v0.1.0から継続する実Chromeのworker scope一覧直接読取と11点1試合完走は、コード・自動検証・公開確認で代替しており、次回必要時に実機確認する。
- 次フェーズへの申し送り: HUD追加はstage相対・pointer-eventsなしを維持し、left railへ必須情報を置かない。版数更新時はapp内cache契約に加え、root+child合成Pages検査もpackage版数と同期させる。
- 関連ドキュメント: [横画面HUDコーナースコア化](./修正設計書_横画面HUDコーナースコア化_2026-08-03.md) / [README](../README.md) / [visual QA](../design-qa.md)
- ステータス更新: 完了（v0.1.1実装・全自動検証・Chrome受入・GitHub Pages公開済み）

### Phase 5: PWA・Pages公開 — 完了（Chrome手動受入一部未了）

- 完了日: 2026-08-03
- 実装内容: 専用PWA identityと横画面icon、`table-tennis2-v0.1.0` Service Worker、既存root workerの子pathname bypass、両アプリをrootと`/table-tennis2/`へ合成するPages build/check、専用PR workflowを実装した。
- 反映コミット: `90d93394d0d26f35628b4afa7cd85a38d4403264`（`table-tennis2-phase5-pages`。Phase 1〜4の先行コミットを含むmaster公開SHA）
- 検証結果: 新規版`npm run check`成功（単体61/61・E2E66/66）、リリース前安定性検査198/198、既存版単一worker E2E66/66、設計書2件のvalidator、合成`_site`検査、`git diff --check`が成功。Actions run `30804672457`はbuild/deploy success、Pages deployment `5724474145`はSHA `90d9339`一致。両URLのHTML・manifest・Service Worker・全iconはHTTP 200・`Ready: true`、両版JS/CSS/SWのSHA-256はローカル合成成果物と一致した。Chrome 844×390で新URLのタイトル、横画面shell、開始操作、サーブ9種×長さ3種UIを確認し、既存URLは従来タイトルと既存戦績を保持、console warning/errorは両URL0件。
- 公開先: 既存 `https://sitar-sitar.github.io/sitar/` / 新規 `https://sitar-sitar.github.io/sitar/table-tennis2/`
- 残課題・既知の問題: Chrome制御の安全ポリシーにより`chrome://serviceworker-internals/`へ遷移できず、実Chrome上のworker scope一覧の直接読取は未実施。登録コードの相対`./sw.js`、root bypass契約、manifestの相対scope、CI、公開worker URLで分離を担保した。新規版の11点1試合完走・戦績保存はChrome手動では未実施で、保存経路は単体/E2Eで確認済み。
- 次フェーズへの申し送り: 横画面固有機能は固定slotと静的FeatureModuleへ追加し、既存game stateへ直接書き込まない。公開時は必ずroot+childの合成artifactを使い、root workerのchild bypassを維持する。
- 関連ドキュメント: [横画面版全体設計](./実装設計書_横画面版全体設計_2026-08-03.md) / [README](../README.md)
- ステータス更新: 完了（v0.1.0実装・自動検証・GitHub Pages公開・主要Chrome確認済み。上記2件のChrome手動確認のみ残課題）

### Phase 4: 拡張スロット

- 完了日: 2026-08-03
- 実装内容: left-rail、right-rail、hud-secondary、overlayの固定slotと、静的FeatureModule、限定command/query、解除関数、ID検証を実装した。サーブ選択イベントをserve-panel featureへ移管した。
- 反映コミット: `bf1a5bb`（`table-tennis2-phase4-features`）
- 検証結果: lint/typecheck成功、単体61件成功。9球種×3配置のサーブ選択E2Eを3ブラウザ・worker 1・retry 0で成功。
- 残課題: PWA/Pages、専用icon、root SW bypass、公開後HTTP/PWA/Chrome受入。
- 次フェーズへの申し送り: 公開artifactは既存rootとtable-tennis2サブパスを必ず合成し、片方だけをdeployしない。
- 関連ドキュメント: [横画面版全体設計](./実装設計書_横画面版全体設計_2026-08-03.md)
- ステータス更新: 完了

### Phase 3: 横画面シェルと入力・描画

- 完了日: 2026-08-03
- 実装内容: compact時のstage+right rail、wide時のleft rail+stage+right rail、safe area対応を実装した。Canvasとrendererはstage実寸を使用し、pointer Xとflick速度もCanvas rectへ正規化した。camera計算を純粋関数へ抽出した。
- 反映コミット: `e8dd2e0`（`table-tennis2-phase3-landscape-shell`）
- 検証結果: lint/typecheck/build成功、単体60件成功。568x320境界でサーブ12ボタンが画面内かつ44x44以上であることを3ブラウザで確認し、全E2E66件をworker 1・retry 0で成功。
- 残課題: FeatureModule、専用icons、root SW bypass、合成Pages workflow、公開・Chrome受入。
- 次フェーズへの申し送り: featureは固定host内だけを変更し、game state更新は限定command経由にする。
- 関連ドキュメント: [横画面版全体設計](./実装設計書_横画面版全体設計_2026-08-03.md)
- ステータス更新: 完了

### Phase 2: 横画面ライフサイクル

- 完了日: 2026-08-03
- 実装内容: viewport境界の純粋評価、portrait/too-small gate、resizeのanimation frame集約、ユーザーpause・document hidden・viewport blockedの停止理由合成を実装した。停止時は入力とAI timerを解除し、全理由解除時のみ時計とtimerを再開する。
- 反映コミット: `8637ac5`（`table-tennis2-phase2-lifecycle`）
- 検証結果: lint/typecheck/build/dist検査成功、単体58件成功、全E2E66件をworker 1・retry 0で成功。並列実行時の短球合成入力1件は単独3反復とworker 1全件で再現せず成功した。
- 残課題: 横画面shell・stage座標、FeatureModule、PWA/Pages公開。
- 次フェーズへの申し送り: gateはviewportのみを変更し、user pauseを解除しない。stageのDOM実寸を入力・描画の単一基準にする。
- 関連ドキュメント: [横画面版全体設計](./実装設計書_横画面版全体設計_2026-08-03.md)
- ステータス更新: 完了

### Phase 1: 独立ベースライン

- 完了日: 2026-08-03
- 実装内容: table-tennis v0.6.2の製品・テスト・PWA基盤を独立コピーし、package、port 3039、IndexedDB `table-tennis2`、cache prefix `table-tennis2-`、PWA名・版数を分離した。テストも新識別子へ更新し、静的契約検査を追加した。
- 反映コミット: `30cee38`（`table-tennis2-baseline`）
- 検証結果: `npm run check` 成功。lint/typecheck/build/dist検査、単体55件、E2E66件すべて成功。初回sandbox内buildはWindows `spawn EPERM` のため失敗し、同一コマンドをsandbox外で再実行して成功した。
- 残課題: Phase 2以降の横画面ライフサイクル、シェル、拡張slot、合成Pages公開。
- 次フェーズへの申し送り: 現行物理・AI・ルールの数値と乱数順を変更せず、viewport依存はview層へ限定する。
- 関連ドキュメント: [横画面版全体設計](./実装設計書_横画面版全体設計_2026-08-03.md)
- ステータス更新: 完了
