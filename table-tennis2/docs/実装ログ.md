# 実装ログ（table-tennis2）

`D:\my-app2\docs\実装フェーズ記録ルール.md` に従い、実装の過程をフェーズ単位で記録する。
次フェーズ着手前に現フェーズを「締めチェックリスト」で必ずクローズする。

- 対象アプリ: table-tennis2
- 現在フェーズ: なし（Phase 5完了・次Phase待ち）
- 現在ステータス: 完了（v0.1.0公開済み・Chrome手動受入一部未了）

---

## 現在進行中フェーズ

なし。

---

## 完了フェーズ（締め済み）

### Phase 5: PWA・Pages公開 — 完了（Chrome手動受入一部未了）

- 完了日: 2026-08-03
- 実装内容: 専用PWA identityと横画面icon、`table-tennis2-v0.1.0` Service Worker、既存root workerの子pathname bypass、両アプリをrootと`/table-tennis2/`へ合成するPages build/check、専用PR workflowを実装した。
- 反映コミット: `90d93394d0d26f35628b4afa7cd85a38d4403264`（`table-tennis2-phase5-pages`。Phase 1〜4の先行コミットを含むmaster公開SHA）
- 検証結果: 新規版`npm run check`成功（単体61/61・E2E66/66）、リリース前安定性検査198/198、既存版単一worker E2E66/66、設計書2件のvalidator、合成`_site`検査、`git diff --check`が成功。Actions run `30804672457`はbuild/deploy success、Pages deployment `5724474145`はSHA `90d9339`一致。両URLのHTML・manifest・Service Worker・全iconはHTTP 200・`Ready: true`、両版JS/CSS/SWのSHA-256はローカル合成成果物と一致した。Chrome 844×390で新URLのタイトル、横画面shell、開始操作、サーブ9種×長さ3種UIを確認し、既存URLは従来タイトルと既存戦績を保持、console warning/errorは両URL0件。
- 公開先: 既存 `https://sitar-sitar.github.io/sitar/` / 新規 `https://sitar-sitar.github.io/sitar/table-tennis2/`
- 残課題・既知の問題: Chrome制御の安全ポリシーにより`chrome://serviceworker-internals/`へ遷移できず、実Chrome上のworker scope一覧の直接読取は未実施。登録コードの相対`./sw.js`、root bypass契約、manifestの相対scope、CI、公開worker URLで分離を担保した。新規版の11点1試合完走・戦績保存はChrome手動では未実施で、保存経路は単体/E2Eで確認済み。
- 次フェーズへの申し送り: 横画面固有機能は固定slotと静的FeatureModuleへ追加し、既存game stateへ直接書き込まない。公開時は必ずroot+childの合成artifactを使い、root workerのchild bypassを維持する。
- 関連ドキュメント: [横画面版全体設計](./実装設計書_横画面版全体設計_2026-08-03.md) / [README](../README.md)
- ステータス更新: 完了（v0.1.0実装・自動検証・GitHub Pages公開・主要Chrome確認済み。上記2件のChrome手動確認のみ残課題）

### Phase 4: 拡張スロット

- 完了日: 2026-08-03
- 実装内容: left-rail、right-rail、hud-secondary、overlayの固定slotと、静的FeatureModule、限定command/query、解除関数、ID検証を実装した。サーブ選択イベントをserve-panel featureへ移管した。
- 反映コミット: `bf1a5bb`（`table-tennis2-phase4-features`）
- 検証結果: lint/typecheck成功、単体61件成功。9球種×3配置のサーブ選択E2Eを3ブラウザ・worker 1・retry 0で成功。
- 残課題: PWA/Pages、専用icon、root SW bypass、公開後HTTP/PWA/Chrome受入。
- 次フェーズへの申し送り: 公開artifactは既存rootとtable-tennis2サブパスを必ず合成し、片方だけをdeployしない。
- 関連ドキュメント: [横画面版全体設計](./実装設計書_横画面版全体設計_2026-08-03.md)
- ステータス更新: 完了

### Phase 3: 横画面シェルと入力・描画

- 完了日: 2026-08-03
- 実装内容: compact時のstage+right rail、wide時のleft rail+stage+right rail、safe area対応を実装した。Canvasとrendererはstage実寸を使用し、pointer Xとflick速度もCanvas rectへ正規化した。camera計算を純粋関数へ抽出した。
- 反映コミット: `e8dd2e0`（`table-tennis2-phase3-landscape-shell`）
- 検証結果: lint/typecheck/build成功、単体60件成功。568x320境界でサーブ12ボタンが画面内かつ44x44以上であることを3ブラウザで確認し、全E2E66件をworker 1・retry 0で成功。
- 残課題: FeatureModule、専用icons、root SW bypass、合成Pages workflow、公開・Chrome受入。
- 次フェーズへの申し送り: featureは固定host内だけを変更し、game state更新は限定command経由にする。
- 関連ドキュメント: [横画面版全体設計](./実装設計書_横画面版全体設計_2026-08-03.md)
- ステータス更新: 完了

### Phase 2: 横画面ライフサイクル

- 完了日: 2026-08-03
- 実装内容: viewport境界の純粋評価、portrait/too-small gate、resizeのanimation frame集約、ユーザーpause・document hidden・viewport blockedの停止理由合成を実装した。停止時は入力とAI timerを解除し、全理由解除時のみ時計とtimerを再開する。
- 反映コミット: `8637ac5`（`table-tennis2-phase2-lifecycle`）
- 検証結果: lint/typecheck/build/dist検査成功、単体58件成功、全E2E66件をworker 1・retry 0で成功。並列実行時の短球合成入力1件は単独3反復とworker 1全件で再現せず成功した。
- 残課題: 横画面shell・stage座標、FeatureModule、PWA/Pages公開。
- 次フェーズへの申し送り: gateはviewportのみを変更し、user pauseを解除しない。stageのDOM実寸を入力・描画の単一基準にする。
- 関連ドキュメント: [横画面版全体設計](./実装設計書_横画面版全体設計_2026-08-03.md)
- ステータス更新: 完了

### Phase 1: 独立ベースライン

- 完了日: 2026-08-03
- 実装内容: table-tennis v0.6.2の製品・テスト・PWA基盤を独立コピーし、package、port 3039、IndexedDB `table-tennis2`、cache prefix `table-tennis2-`、PWA名・版数を分離した。テストも新識別子へ更新し、静的契約検査を追加した。
- 反映コミット: `30cee38`（`table-tennis2-baseline`）
- 検証結果: `npm run check` 成功。lint/typecheck/build/dist検査、単体55件、E2E66件すべて成功。初回sandbox内buildはWindows `spawn EPERM` のため失敗し、同一コマンドをsandbox外で再実行して成功した。
- 残課題: Phase 2以降の横画面ライフサイクル、シェル、拡張slot、合成Pages公開。
- 次フェーズへの申し送り: 現行物理・AI・ルールの数値と乱数順を変更せず、viewport依存はview層へ限定する。
- 関連ドキュメント: [横画面版全体設計](./実装設計書_横画面版全体設計_2026-08-03.md)
- ステータス更新: 完了
