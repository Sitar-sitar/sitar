# 実装ログ（table-tennis）

`D:\my-app2\docs\実装フェーズ記録ルール.md` に従い、実装の過程をフェーズ単位で記録する。
**次フェーズ着手前に現フェーズを「締めチェックリスト」で必ずクローズする。**

- 対象アプリ: table-tennis
- 現在フェーズ: Phase 9
- 現在ステータス: 進行中

---

## 現在進行中フェーズ

### Phase 9: v0.6.1 台上技術の成立性再設計

- 目的 / スコープ: STOPのノーリスク化（A-1）とFLICKの打点距離逆依存（A-2）を、固定速度＋後付け補正（S13・S14）から`ShotSpeed`判別共用体＋必要速度モデルへ置き換えて解消する。あわせて`SHORT_BOUNCE_Z`を58→48へ修正（A-3）、台上局面の終了性を決定論的性質と統計回帰へ分けて自動検証する（A-4）。
- 対象バージョン: v0.6.1（SW `table-tennis-v8`）
- 関連設計書: [修正設計書_台上技術の成立性再設計_2026-07-30](./修正設計書_台上技術の成立性再設計_2026-07-30.md)（関連レビュー: [設計レビュー_台上技術の成立性再設計_2026-07-30](./設計レビュー_台上技術の成立性再設計_2026-07-30.md)。P1 2件・P2 4件・P3 1件を全件反映済み・未解決0件）
- 起点コミット: `9e6e70c`（`master`。v0.6.0公開済み・G3完了）
- 着手日: 2026-07-30
- ステータス: 進行中

**作業メモ（随時追記）**

- 2026-07-30: Obsidian `table-tennis 開発・設計レビュー注意点.md` を全文参照してから着手。design-implスキルの正本（プロジェクトCLAUDE.md §4〜6・§8、実装フェーズ記録ルール）を確認し、Phase 9を起こした。垂直スライスS0〜S5（設計書§7）の順で進める。
- 2026-07-30: S0着手前に、設計書§5.1のU33格子（非対象5技×両方向×打点×3乱数列＝72ベクトル）を、現行コード（`9e6e70c`）に対して一時スクリプトで実測した。打点B・乱数全0の10組は設計書§4.2の照合用スポット値と完全一致を確認した（DRIVE/SMASH/PUSH/CHOP/LOBの±両方向）。
- 2026-07-30: S0（U33追加、製品コード無変更）を`f6824d8`としてコミット。単体46・E2E57がgreen。
- 2026-07-30: S1（`ShotSpeed`判別共用体・`SHOTS`7件移行・`solveShot()`再構成・S13/S14撤去・U28'への置換）を実装。U28'のサブケースa〜dは、設計書§5.2の実測値・エンベロープと一時スクリプトで突き合わせて校正した。depth/aimXは各試行ごとにAI実装と同じ式で乱数抽選する（固定代表値ではない）ことを実測で確認した実装方針として採用し、§5.2の実測表（STOP/FLICKのミス率・自陣落下率・着地percentile・blunder分離）と概ね1pt以内で一致することを検証した。`npm run typecheck`はpass（設計書§4.2の完成形どおり）。
- 2026-07-30: `npm run check`のE2Eで`[mobile-chromium] 短い球に合成フリックで台上技術が出る`が既定の8並列実行で断続的に失敗した。S1直前（S0コミット時点）のコードへ一時的に戻して同条件で再現することを確認し、本セッションの実行環境におけるCPU競合（E14と同種の合成ジェスチャータイミング問題）であって、S1の製品変更由来ではないと特定した。`--workers=3`に落とすと同一テストが安定してpassすることも確認した。以後のスライスは`npm run test:e2e`相当を`npx playwright test --workers=3`で検証し、その旨をログへ明記する。
- 2026-07-30: S2（`solveContactPlane()`の抽出・`Game.contactPlane()`の委譲化・C9追加）を実装。`Game.contactPlane()`/`clampPlane()`の式・評価順・3秒上限・`PZ`/`AZ`フォールバックをそのまま`src/physics.ts`へ移し、`cloneBall()`で安全に複製してから可変状態を扱う。C9は`contactPlane()`内で`integrate()`を直接呼ぶ形へ違反注入してfailすることを実地確認済み。`npm run check`は単体46・E2E57（`--workers=3`）でgreen。

## 完了フェーズ（締め済み）

### Phase 8: v0.6.0 台上技術 — 完了

- 目的 / スコープ: 打球解決処理を挙動不変で純粋関数化したうえで短球判定を追加し、プレイヤーとAIへストップ／フリックを実装する。操作説明、版数、Service Worker、E2Eを更新し、最終公開へ進める。
- 対象バージョン: v0.6.0
- 関連設計書: [機能設計書_サーブ拡張と台上技術_2026-07-30](./機能設計書_サーブ拡張と台上技術_2026-07-30.md)（Phase 3）
- 起点コミット: `7515ee8`（Phase 7 G1締め）
- 着手日: 2026-07-30
- 完了日: 2026-07-30
- ステータス: 完了（G1・G3到達、GitHub Pages公開済み）

**作業メモ**

- 2026-07-30: Phase 7の締めチェックリストを全項目確定し、Phase 8へ着手。S9〜S12を順に実装し、各スライス末尾で `npm run check` と独立コミットを行う。
- 2026-07-30: S9（`solveShot` の挙動不変抽出）を `4a8cc97`、S10（`lastBounceZ`、短球判定、STOP/FLICK、C7/C8、U25〜U28）を `f777c79` として独立コミット。S9は単体40件・E2E54件、S10は単体44件・E2E54件の `npm run check` をpassした。
- 2026-07-30: S11着手前に、AI短球経路のaim / blunderが独立乱数を必要とする一方、正本が総消費を2回へ固定している矛盾を検出。推測実装を避けて停止し、ユーザー承認後に正本を全難易度共通4回（種別→aim→depth→blunder）へ修正した。長球経路の乱数順は変更しない。
- 2026-07-30: 続く数値確認で、AI STOP の式 `26 × (0.9 + 0.2 × r)` に対する記載範囲 `23.4〜31.2` が誤りと判明。実装式は変更せず、正本の派生範囲とU29期待値を正しい `23.4〜28.6` へ訂正した。
- 2026-07-30: S11（AI台上判断・U29）を `18d1aec`、S12（操作説明・E14・v0.6.0・SW v7）を `f81c91a` として独立コミット。S12最終 `npm run check` は単体45件・E2E57件をpassした。
- 2026-07-30: Chrome G1でAI STOPの選択表示後にプレイヤー得点となる不成立を検出。短いナックルサーブの実AI打点 `z=75.87 / y=14.98` では、設計速度230〜270の基準弾道が10,000/10,000でネットへ届かないことを純粋物理で再現した。計画から外れる理由を説明し、近距離STOPや他ショットを変えない条件付き速度フォールバックをS13として正本へ追加した。
- 2026-07-30: S13を `816cca8` としてコミット後、Chrome G1再確認でSTOP選択後の自陣落下を2回連続で再現した。上級AIの明示的ミス率は2%であり偶発ミスだけでは説明できないため、最低接触品質 `0.25` と標準誤差端を総当たりし、128通り中52通りが不成立と特定。速度余裕の単純増加はSTOP帯を越えるため採用せず、非blunder時だけ誤差後の最終帯を再検査するS14を正本へ追加した。
- 2026-07-30: S14を `0ea2a92` として実装。非blunder STOPだけを誤差適用後に再検査し、ネット・自陣・`abs(z)=18〜46` 帯外なら追加乱数なしで再解決する。U28を実AI打点、hard aim/depth端、接触品質 `1 / 0.25`、標準誤差3軸端へ拡張し、明示的blunderが補正対象外であることも固定した。
- 2026-07-30: 最初のS14後 `npm run check` は、製品・単体ではなくE14のdesktop-chromium 1件だけが高負荷時に失敗した。反復ジェスチャーの120ms待機によりフリック有効時間160msの間へ空白ができることを原因と特定し、製品コードを変えず反復間を10ms、最大24回へ変更した（`b21b872`）。desktop-chromium 4並列10回反復は10/10、続く全チェックは57/57 passした。
- 2026-07-30: Chromeでプレイヤーの下フリックSTOP、上フリックFLICK、長球の下フリックが従来どおりツッツキになることを確認。S14後の新規オリジンでは、短いナックルサーブからAIが `RALLY 1 / ストップ` を出し、約0.8秒後に「返せず」でAI得点へ進んだ。320×240はサーブパネル `y=7..230`、320×480は `y=238..470` に収まり、制御済みService Workerでのオフライン起動とconsole error 0件も確認した。
- 2026-07-30: G3初回run `30507701498`（SHA `8326582`）はbuild（E2E57件）とdeployがsuccessしたが、`actions/configure-pages@v5` のNode.js 20非推奨警告が残った。G3条件を満たさないため公開完了とはせず、公式release `v6.0.0` と `v6/action.yml` の `runs.using: node24` を確認して、該当1行だけを更新するS15を正本へ追加した。
- 2026-07-30: S15を `e2bc9a2` で実装し、`actions/configure-pages@v6` へ更新。Actions run `30507958371` はbuild（単体45件・E2E57件）/ deployともsuccessし、Pages deployment `5668004761` のSHAは `e2bc9a28e64e06beb37066327fdad4bf8f24e721` と一致した。全ログの文字列検査でNode 20非推奨警告0件を確認した。
- 2026-07-30: `verify-live-pages.ps1` はReady=true。HTML / manifest / Service Worker / JS / CSS / アイコンが全てHTTP 200で、HTML / JS / CSS / Service Worker / アイコンはローカル `dist` とSHA-256一致。manifestはローカルCRLF・公開LFの改行差だけで、正規化後に完全一致した。公開Chromeで再読み込み後のv0.6.0、9球種、3長さ、試合開始、実サーブから `RALLY 1`、console error 0件を確認した。

**締めチェックリスト**

- 完了日: 2026-07-30
- 実装した内容: `solveShot` の挙動不変抽出、`lastBounceZ` と短球判定、プレイヤー／AIのSTOP・FLICK、技別AI depth、純粋関数とC7/C8、操作説明、v0.6.0、Service Worker v7を実装した。実機で判明した遠距離AI STOPをS13/S14の2段階で補正し、非blunder時の最終着地点帯を保証した。S15でPages設定ActionをNode.js 24対応の`configure-pages@v6`へ更新した。
- 反映コミット: `4a8cc97`（S9）/ `f777c79`（S10）/ `18d1aec`（S11）/ `f81c91a`（S12）/ `816cca8`（S13）/ `0ea2a92`（S14）/ `e2bc9a2`（S15）。E14安定化は `e0cbf83` / `b21b872`。
- 検証結果: 最終 `npm run check` pass。lint、厳格TypeScript、PWAソース契約、Vite build、`dist`契約、単体45件、3環境E2E57件がgreen。U28は非blunder STOPの実AI最低品質・誤差端と明示的blunder除外を検査し、U29、C7/C8もpass。E14はdesktop-chromium 4並列10回反復で10/10。Chromeで§8.3 Phase 3と共通確認を実施し、AI STOPは `RALLY 1` からAI得点へ到達、境界ビューポート、オフライン、console error 0件を確認した。公開run `30507958371`、deployment `5668004761`、資産一致、公開Chrome確認もpassし、G3完了。
- 残課題・既知の問題: 実装範囲の既知問題なし。`actions/deploy-pages@v5` 内部のNode.jsが`[DEP0040] punycode`非推奨警告を1件出すが、本設計で解消対象としたNode 20ランタイム警告ではなく、build / deploy / 公開資産に影響しない外部Action由来の警告として記録する。新規オリジン3039の初回Service Worker制御前にサーバーを止めた再読み込みは静的HTMLだけとなったため合格証跡には数えず、制御済み3038とE2Eのオフライン確認を採用した。未実施の必須検証はなし。
- 次フェーズへの申し送り: 回帰基準は単体45件・E2E57件。9球種×3長さの物理成立性、AI乱数順、STOP最終帯、build済み`dist`に対するオフラインE2E、PagesのNode.js 24 Action契約を維持する。
- 関連ドキュメント: [機能設計書](./機能設計書_サーブ拡張と台上技術_2026-07-30.md) / [設計レビュー](./設計レビュー_サーブ拡張と台上技術_2026-07-30.md) / [設計書インデックス](./設計書インデックス.md) / [README](../README.md)
- Obsidian更新: 済（`Projects/my-app2 プロジェクト一覧.md` のtable-tennis節へv0.4.1〜v0.6.0の公開結果を追記）
- ステータス更新: 完了（G1・G3到達、GitHub Pages公開済み）。親台帳は `5ef4c9c` で更新・push済み。

---

### Phase 7: v0.5.0 サーブ9種・長短 — 完了

- 目的 / スコープ: サーブを縦回転×横回転の9種へ拡張し、短い・中・長いの3段階を追加する。要求値と解決値を区別したフォールバック、3×3 UI、AIの長さ選択、短球を返せる打点平面を実装する。
- 対象バージョン: v0.5.0
- 関連設計書: [機能設計書_サーブ拡張と台上技術_2026-07-30](./機能設計書_サーブ拡張と台上技術_2026-07-30.md)（Phase 2）
- 起点コミット: `96145ba`（Phase 6 G1締め）
- 着手日: 2026-07-30
- 完了日: 2026-07-30
- ステータス: 完了（G1・G3到達、v0.6.0と一括公開済み）

**作業メモ**

- 2026-07-30: Phase 6の締めチェックリストを全項目確定し、Phase 7へ着手。S4〜S8を順に実装し、各スライス末尾で `npm run check` と独立コミットを行う。
- 2026-07-30: S4（サーブ4球種追加・9種のAI球種重み）を `600fc2a`、S5（3長さの物理プロファイル・162通り成立性と単調性）を `727dad3` として独立コミット。各スライス末尾の `npm run check` はgreen。
- 2026-07-30: S6着手時に、正本が `chooseServeLength` と「難易度ごとの重み」を要求する一方、9個の数値を定義していないことを検出し、推測を避けて一時停止した。ユーザー指示に基づく推奨値として、`short/middle/long` の順に easy=`5/80/15`、mid=`25/50/25`、hard=`40/30/30` を正本 §4.2.2 へ確定した。既存の `AI_SERVE_WEIGHTS` と所有先を揃えるため、S6対象に `src/config.ts` を追加した。
- 2026-07-30: S6（AI長さ選択・要求値/解決値・5段フォールバック）を `5fc11dd`、S7（9×3 UI・選択保持・C5/C6・E12/E13）を `cc4f89f`、S8（打点平面・版数・SW・README）を `52dfff0` として独立コミット。C5/C6は要素を1件減らした負例で意図どおりfailすることも確認した。
- 2026-07-30: 最終 `npm run check` は lint・厳格TypeScript・PWAソース契約・Vite build・`dist`契約・単体40件・3環境E2E 54件をすべてpass。U21は162通りをフォールバック無しで成立、U22は54組の実測帯と `short < middle < long` を確認した。
- 2026-07-30: Chromeで320×480の9球種3×3＋長さ3ボタンが `x=10..310 / y=238..470` に収まり、全ボタンが44px高で操作可能なことを確認。短い横上左サーブと長い横下右サーブを実行し、どちらもAIが返球して `RALLY 1` へ進んだ。上級AIの初回サーブ5本で `long / middle / short / short / short` を観測し、短・中・長の混在を確認した。320×240 / 320×480のCanvas収まりと待機ラケット、プレビュー停止後のオフライン起動、console error 0件も確認した。

**締めチェックリスト**

- 完了日: 2026-07-30
- 実装した内容: 既存5種を含む縦回転×横回転9種、短い・中・長いの物理プロファイル、難易度別AI長さ重み、要求値と解決値を分離した5段フォールバック、9×3の選択UI、ポイント・再試合をまたぐ選択保持を実装した。`CONTACT_PLANE_NEAR` を30へ変更し短いサーブを返球可能にした。版数をv0.5.0、SWキャッシュをv6へ更新した。
- 反映コミット: `600fc2a`（S4）/ `727dad3`（S5）/ `5fc11dd`（S6）/ `cc4f89f`（S7）/ `52dfff0`（S8）。AI長さ重みの正本確定は `1b5f9cc`。
- 検証結果: 最終 `npm run check` pass。lint、厳格TypeScript、PWAソース契約、Vite build、`dist`契約、単体40件、3環境E2E 54件がgreen。C5/C6の負例もfailを確認。Chromeで設計書 §8.3 Phase 2と共通確認を実施し、9×3 UI、長さトグル、複合回転サーブ、短長サーブのAI返球、上級AIの短中長混在、境界ビューポート、待機ラケット、オフライン、console error 0件を確認した。
- 残課題・既知の問題: 実装範囲の既知問題なし。G3はrun `30507958371` / deployment `5668004761` で完了。Chrome制御環境ではCanvasピクセル書き出しAPIを利用できなかったため、複合回転の曲がりと落下の定量確認はU20/U21/U22の物理テストで担保し、Chromeでは複合回転2種のサーブ成立とAI返球を確認した。未実施の必須検証はなし。
- 次フェーズへの申し送り: 回帰基準は単体40件・E2E54件。S9は挙動不変の`solveShot`抽出だけを行い、乱数消費順（side→非LOB速度→仰角誤差→方位角誤差→速度誤差）を維持してから、S10で短球判定とSTOP/FLICKを追加する。
- 関連ドキュメント: [機能設計書](./機能設計書_サーブ拡張と台上技術_2026-07-30.md) / [設計レビュー](./設計レビュー_サーブ拡張と台上技術_2026-07-30.md) / [設計書インデックス](./設計書インデックス.md) / [README](../README.md)
- Obsidian更新: 済（最終公開時にv0.4.1〜v0.6.0を一括反映）
- ステータス更新: 完了（G1・G3到達、v0.6.0と一括公開済み）

---

### Phase 6: v0.4.1 基盤整理 — 完了

- 目的 / スコープ: ショット種別と持ち手傾きの対応を一元化し、サーブ待機高さ、BallState、ショット設定、物理積分の不要状態を整理する。bfcache 復帰時の AI サーブ二重発火を防ぎ、GitHub Pages の deploy action を Node.js 24 対応版へ更新する。
- 対象バージョン: v0.4.1
- 関連設計書: [機能設計書_サーブ拡張と台上技術_2026-07-30](./機能設計書_サーブ拡張と台上技術_2026-07-30.md)（Phase 1）
- 関連レビュー: [設計レビュー_サーブ拡張と台上技術_2026-07-30](./設計レビュー_サーブ拡張と台上技術_2026-07-30.md)（判定: 実装着手可）
- 起点コミット: `be12eeb`（`master`。設計レビュー文書更新済み）
- 着手日: 2026-07-30
- 完了日: 2026-07-30
- ステータス: 完了（G1・G3到達、v0.6.0と一括公開済み）

**作業メモ**

- 2026-07-30: ユーザー指示により Phase 1〜3 の実装と最終 Web 公開を開始。設計書 §6 の垂直スライス S1〜S12 を順守し、各スライス末尾で `npm run check` とコミットを実施する。
- 2026-07-30: S1（`swingTypeOf`）を `99f968a`、S2（サーブ状態・物理積分・bfcache復帰）を `55ee4c4` として独立コミット。各スライス末尾の `npm run check` は単体37件・E2E48件で pass。
- 2026-07-30: `actions/deploy-pages@v5` の公式 `action.yml`（<https://raw.githubusercontent.com/actions/deploy-pages/v5/action.yml>）を確認し、2026-07-30 時点で `runs.using: node24` であることを確認した。ワークフローは deploy step の参照だけを `@v4` から `@v5` へ更新し、build/deploy job 構造は維持した。
- 2026-07-30: S3を `08cbccd` として独立コミット。最終 `npm run check` は lint・厳格TypeScript・PWAソース契約・Vite build・`dist`契約・単体37件・3環境E2E 51件をすべて pass。E11 は700msのAIサーブタイマーの実発火回数を計測し、`pagehide` → `pageshow` 後も1回だけであることを確認した。
- 2026-07-30: Chromeで320×480のサーブ操作領域が `(x=10, y=338, width=300, height=132)` に収まり、320×240ではCanvasが320×240を満たしてラケットのブレード・持ち手とも欠けないことを確認。通常ドライブと押し出しを同一セッションで発生させ、ラケットの振り上げと中間動作を直接確認した。プレイヤーの5種サーブ操作、待機からサーブ直後の高さジャンプなし、AIサーブ後のラリー、実ナビゲーション復帰後のAIサーブ1回、プレビュー停止後のオフライン起動（タイトル・ゲスト・Canvas）、コンソール error/warn 0件も確認した。

**締めチェックリスト**

- 完了日: 2026-07-30
- 実装した内容: `swingTypeOf` を純粋関数化して `DRIVE` / `SMASH` を振り上げへ統一。サーブ待機高さを `SERVE_CONTACT_Y=24` へ一本化し、bfcache復帰時のAIサーブ再スケジュールとタイマー単一化を実装した。`BallState.bSide`、`ShotConfig.pw`、物理積分のモジュール可変状態を削除。Pages deploy actionをv5、版数をv0.4.1、SWキャッシュをv5へ更新した。
- 反映コミット: `99f968a`（S1）/ `55ee4c4`（S2）/ `08cbccd`（S3）。
- 検証結果: 最終 `npm run check` pass。lint、厳格TypeScript、PWAソース契約、Vite build、`dist`契約、単体37件、3環境E2E 51件がgreen。公式 `actions/deploy-pages@v5/action.yml` の `runs.using: node24` を確認。Chromeで設計書 §8.3 Phase 1と共通確認を実施し、境界ビューポート・通常ドライブ対押し出し・サーブ高さ・AIサーブ復帰・既存ラリー・オフライン・コンソール error/warn 0件を確認した。
- 残課題・既知の問題: 実装範囲の既知問題なし。G3はrun `30507958371` / deployment `5668004761` で完了し、全ログでNode 20非推奨警告0件を確認した。未実施の必須検証はなし。
- 次フェーズへの申し送り: 回帰基準は単体37件・E2E51件。`middle` のサーブ物理はv0.4.1とビット単位で一致させ、AI乱数消費順は球種→長さ→狙いとする。各スライス末尾の全チェックと独立コミットを継続する。
- 関連ドキュメント: [機能設計書](./機能設計書_サーブ拡張と台上技術_2026-07-30.md) / [設計レビュー](./設計レビュー_サーブ拡張と台上技術_2026-07-30.md) / [設計書インデックス](./設計書インデックス.md)
- Obsidian更新: 済（最終公開時にv0.4.1〜v0.6.0を一括反映）
- ステータス更新: 完了（G1・G3到達、v0.6.0と一括公開済み）

---

### Phase 5: v0.4.0 ラケットの持ち手と奥行き表現 — 完了

- 目的 / スコープ: プレイヤー・相手ラケットへ持ち手（グリップ）を描画し、プレイヤーラケットの縦位置・大きさ・影を打点の前後位置に連動させる。打球判定は変更しない。
- 対象バージョン: v0.4.0
- 関連設計書: [機能設計書_ラケットの持ち手と奥行き表現_2026-07-29](./機能設計書_ラケットの持ち手と奥行き表現_2026-07-29.md)（ステータス: 実装済み・公開済み）
- 関連レビュー: [設計レビュー_ラケットの持ち手と奥行き表現_2026-07-29](./設計レビュー_ラケットの持ち手と奥行き表現_2026-07-29.md)（初回判定: 着手不可。P1 1件・P2 4件・P3 1件を設計へ全件反映してから着手）
- 起点コミット: `b644077`（`master`。Phase 4 の成果。v0.3.0 公開済み）
- 着手日: 2026-07-29
- 完了日: 2026-07-29
- ステータス: 完了

**作業メモ（随時追記）**

- 2026-07-29: ユーザー指示により実装・Web公開フェーズを開始。設計書 §6 の垂直スライス S1〜S4 の順で進める。回帰基準は Phase 4 の申し送りどおり単体29件・E2E 48件。
- 2026-07-29: S1（持ち手）を実装。共有の `paddle()` へ向き引数を足し、持ち手を先に描いてブレードで根元を隠す描画順にした。相手側は手から腕の付け根への角度を渡す。小画面対策の `clampPaddleScreenY` もこのスライスに含めた。S1 時点で lint・typecheck・単体31件 pass を確認した。
- 2026-07-29: S2（奥行き）を実装。`viewZ` の追従、奥行き比による縦位置・大きさ・影の連動、`projectScale` の切り出しを入れた。横位置は判定平面 `player.z` のままとし、レビュー P1-01 の契約を守った。
- 2026-07-29: ソース契約検査 C1〜C4 を `check-app.mjs` へ追加し、**4件それぞれについて違反を注入すると `check:app` が fail することを実地確認**した（横投影に `viewZ` を渡す／`startServe` のスナップ削除／`tick` の追従削除／`ai.ts` に `stepViewZ` を書く）。確認後はソースを復元し、typecheck の green を再確認した。
- 2026-07-29: `sw.js` を `table-tennis-v4` へ上げたところ、E2E の `game-smoke.spec.js` がキャッシュ名 `table-tennis-v3` を直書きしていたため WebKit の1シナリオが fail した。`caches.keys()` から接頭辞一致で導出する形へ是正し、以後のキャッシュ名更新で落ちないようにした（設計書 §4.7 へ反映）。
- 2026-07-29: 最終 `npm run check` が lint・厳格TypeScript・PWAソース契約・Vite build・`dist`契約・単体35件・3環境E2E 48件をすべて pass した。
- 2026-07-29: Chrome 実機確認を実施（`npm run build` ＋ `npm run preview`）。旧 v3 Service Worker がキャッシュ済みシェルを返す既知の経路を踏んだため再読込し、v4 更新後に持ち手つきラケット、奥行きに応じた上下・大小変化、点間スナップ、相手ラケットの持ち手、サーバー停止時のオフライン起動、戦績表示、コンソール error 0件を確認した。境界ビューポートは Canvas を強制サイズにしてピクセル走査で最下端を実測し、320×240 と 320×200 で余白が設計値ちょうど 4px、320×480 と 568×320 で欠けなしを確認した。詳細な数値は設計書 §8.3 の実施結果表に記録した。
- 2026-07-29: `283fef7` を `origin/master` へ push。Actions run `30450622807` の build（CI 側の `npm run check` を含む）と deploy が success し、Pages deployment `5657133229` の SHA が `283fef7` と一致した。公開 HTML・manifest・Service Worker・JS・CSS・全アイコンは HTTP 200 で、HTML/JS/CSS/SW/manifest はローカル `dist` と SHA-256 一致。公開 Chrome では初回表示が旧 Service Worker のキャッシュ（v0.3.0 の描画）だったため再読込し、更新後に持ち手と奥行き表現、コンソール error 0件を確認した。親リポジトリの `PROJECT_OVERVIEW.md` は `66681b1` で更新・push 済み。

**実装中に記録した設計調整**

実装中に設計書本文へ反映した変更は次の2件。いずれも設計書側が正本であり、本ログでは要点のみ記録する。

- **E2E がキャッシュ名を直書きしていたため導出方式へ是正した**（設計書 §4.7）。設計は「`APP_SHELL` の内容が変わらないため既存検査はそのまま通る」としていたが、`tests/game-smoke.spec.js` の WebKit 経路が `table-tennis-v3` を直書きしており、`v4` へ上げた時点で当該シナリオが fail した（空のキャッシュを新規作成するため）。`caches.keys()` から接頭辞一致で導出する形へ改め、以後のキャッシュ名更新で落ちないようにした。シナリオ数は 48 件のまま。
- **S1 と S2 のコミットが 1 本へ統合された**（設計書 §6・§7.2）。設計は S1（持ち手）と S2（奥行き）を別コミットにして「奥行きだけを revert できる」ことを意図していたが、S1 完了時点でコミットせずに S2 へ進んだため、同一ファイル内で変更が入り組み安全な分割ができなくなった。§7.2 のロールバック表を、統合コミットを前提に「奥行きのみを手作業で戻す対象一覧」へ改訂した。**次フェーズ以降はスライス末尾で必ずコミットする。**

---

**締めチェックリスト**

- 完了日: 2026-07-29
- 実装した内容: 共有 `paddle()` へ持ち手（グリップ）を追加し、プレイヤーはスイング種別で傾け、相手は手から腕の付け根へ向けた。描画専用の `PaddleState.viewZ` を追加し、プレイヤーラケットの縦位置・大きさ・影を打点の前後位置へ連動させた。横位置は判定平面 `player.z` の投影のままとし、打球判定は無変更。小画面向けに持ち手を含む外接下端を Canvas 内へ制限し、`projectScale` の切り出しとソース契約検査 C1〜C4 を追加した。版数を v0.4.0、Service Worker を `table-tennis-v4` へ更新した。
- 反映コミット: `23356c6`（S1・S2 コード）/ `de4ba7d`（S3 版数・SW・README・E2E是正）/ `283fef7`（S4 文書）。親リポジトリ台帳は `66681b1`。
- 検証結果: 最終 `npm run check` pass。lint、厳格TypeScript、PWAソース契約、Vite build、`dist`契約、単体35件、3環境E2E 48件がすべて green。ソース契約検査 C1〜C4 は違反を注入して4件とも fail することを実地確認。Chrome 実機で持ち手・奥行き・点間スナップ・相手ラケット・オフライン起動・戦績表示・コンソール error 0件を確認し、境界ビューポート4条件（320×240 / 320×480 / 568×320 / 320×200）はピクセル走査で最下端を実測して欠けなしを確認した。公開は Actions run `30450622807`、Pages deployment `5657133229`、公開資産の SHA-256 一致、公開 Chrome 確認まで完了。
- 残課題・既知の問題:
  - スマッシュ（`+0.5rad`）とツッツキ（`-0.3rad`）の持ち手の傾きは、実描画のピクセル計測では捕捉できていない（任意のショット種別を実機で発生させられないため）。方向と大小は単体テスト U15 で担保している。
  - 11点までの1試合完走による戦績記録は未実施。本フェーズは記録経路を変更しておらず、v0.3.0 §8.4-5 と既存 E2E で担保している。
  - Phase 3 から継続する3件（サーブ表示高さ26と物理解起点24の2単位差、`physics.ts` の共有 `acceleration`、iOS Safari bfcache復帰時のAIサーブタイマー）は本フェーズのスコープ外として未修正。
- 次フェーズへの申し送り:
  - 回帰基準は**単体35件・E2E 48件**（Phase 4 時点の29件・48件は上書き済み）。`OpponentAi` の乱数消費順／判断契約も引き続き維持する。
  - **ラケットの横位置に `viewZ` を使ってはならない**（打球判定と画面上位置がずれる）。`check:app` の C1 がこの契約を守っている。C2〜C4 も含め、描画の結線を変えるときは検査側も同時に見直す。
  - Service Worker のキャッシュ名を上げるときは、E2E がキャッシュ名を直書きしていないか確認する（v0.4.0 で導出方式へ是正済み）。
  - **垂直スライスは末尾で必ずコミットする**。今回 S1 と S2 を分けられず、設計の部分ロールバック手順を書き換える必要が生じた。
  - 表示定数（`PADDLE_DEPTH_RISE` / `PADDLE_DEPTH_SHRINK` / `P_DEPTH_SPEED` / 持ち手4件 / `PADDLE_EDGE_MARGIN`）は今回調整していない。変更する場合は §4.2.6 の画面内制限と U17 の期待値を満たすこと。
  - プレイヤーの能動的な前後移動は未実装（設計書 §10-3）。実装するなら縦ドラッグがショット判定に使われているため、入力チャネルの選定から別設計を起こす。
- 関連ドキュメント: [機能設計書](./機能設計書_ラケットの持ち手と奥行き表現_2026-07-29.md) / [設計レビュー](./設計レビュー_ラケットの持ち手と奥行き表現_2026-07-29.md) / [設計書インデックス](./設計書インデックス.md) / [README](../README.md)
- Obsidian更新: 済（`Projects/my-app2 プロジェクト一覧.md` の一覧行と table-tennis 節へ v0.4.0 と設計判断3件を追記）
- ステータス更新: 完了（GitHub Pages公開済み・次フェーズ待ち）

---

### Phase 4: v0.3.0 プレイヤー戦績とラケット表示改修 — 完了

- 着手準備完了日: 2026-07-28
- 実装着手日: 2026-07-28
- 完了日: 2026-07-28
- 起点コミット: `0a8d5f8`（`master`。第2期リファクタリング統合コミット。Phase 3 の成果）
- 設計確定コミット: `ccb62cf`（設計レビュー反映・再レビュー結果。ドキュメントのみ。先行 push 済み）
- 反映コミット: `a7fa030`（Phase A: ラケット表示）/ `fa2b12a`（Phase B: プレイヤー戦績・UI・PWA・文書）
- 設計正本: [機能設計書_プレイヤー戦績とラケット表示改修_2026-07-27](./機能設計書_プレイヤー戦績とラケット表示改修_2026-07-27.md)（ステータス: 実装済み・公開済み）
- レビュー: [設計レビュー_プレイヤー戦績とラケット表示改修_2026-07-28](./設計レビュー_プレイヤー戦績とラケット表示改修_2026-07-28.md)（判定: 着手可。未解決 P1・P2・P3 は0件）

**作業メモ（随時追記）**

- 2026-07-28: ユーザー指示により実装・Web公開フェーズを開始。最初に設計確定文書を `ccb62cf` として `origin/master` へ push し、Actions run `30359181193` の成功を確認した。
- 2026-07-28: 公開基盤の現行値を再確認。public repository、Pages `build_type: workflow`、Vite `dist/` の build-gated 配布、相対PWAパス、危険なtrackedファイルなしを確認した。
- 2026-07-28: S1で腕を廃止し、画面下部の独立ラケット、影、同心円を実装した（`a7fa030`）。同スライス末尾の `npm run check` は単体19件・E2E 18件を含めてpassした。
- 2026-07-28: S2で `maxRally` を `point()` 更新・`newGame()` リセットへ変更し、S3で戦績型、集計純粋関数、Unicodeコードポイント対応、単体テスト10件を追加した。S3末尾の単体テストは設計期待値どおり29件passした。
- 2026-07-28: S4でIndexedDBの `players` / `matches` / `settings`、ID多段フォールバック、ゲスト作成、CRUD、削除時の戦績連鎖削除を実装した。S5〜S6でプレイヤー／戦績UI、試合終了スナップショット、保存世代管理、保存失敗時のゲーム継続、Service Worker `table-tennis-v3`、E2E 10シナリオを追加した。
- 2026-07-28: S7まで反映した最終 `npm run check` はlint、厳格TypeScript、PWAソース契約、Vite build、`dist`契約、単体29件、デスクトップChromium／iPhone相当WebKit／Pixel 7相当ChromiumのE2E 48件をすべてpassした。
- 2026-07-28: ローカルChrome実操作でService Worker v3への更新、新規プレイヤー追加・選択、HUD連動、戦績表示、320×568の各オーバーレイ収まり、独立ラケットの表示と横移動、コンソールerror/warn 0件を確認した。
- 2026-07-28: ローカルChromeで中級を2試合連続して完了し、通算が0勝1敗→0勝2敗へ増加、直近履歴が2件、最大ラリーが1試合目3回・2試合目2回となり、前試合値が持ち越されないことを確認した。
- 2026-07-28: `dist` のサーバー停止再読込で、旧Service Workerがキャッシュ済み資産にも並行ネットワーク要求を出すためJS/CSSを起動できないケースを検出した。`sw.js` を厳密なcache-firstへ修正し、E2EをVite開発サーバーではなくbuild済み `dist` プレビュー対象へ変更した。修正後は実Chromeでサーバー停止中も「ゲスト」と戦績機能を含めて起動し、Chromium 2構成のオフライン再読込とWebKitのアプリシェルキャッシュ検査を含むE2E 48件がpassした。
- 2026-07-28: `fa2b12a` を `origin/master` へ push。Actions run `30363050159` のbuild/deployがsuccessし、Pages deployment `5640592726` のSHAが `fa2b12a` と一致した。公開HTML、manifest、Service Worker、JS、CSS、全アイコンはHTTP 200。HTML/JS/CSSはローカル `dist` とSHA-256一致し、Service Worker/manifestもWindowsとActionsの改行差をLFへ正規化後に一致した。
- 2026-07-28: 公開Chromeで旧v2 Service Workerからv3への更新と再読み込み後、`ゲスト` 初期化、プレイヤー `公開確認` の追加・選択、HUD名の反映を確認した。公開資産は検証済みローカル `dist` と一致する。
- 2026-07-28（締め後の追補）: 実装ログのレビューで、設計書 §6 S4 が定める `scripts/check-app.mjs` の「`index.html` の戦績UI要素の存在検査」が未実装であることを検出した。`#playerBar` / `#playerName` / `#playerRecord` / `#hudPlayerName` / `#players` / `#stats` / `#rRecord` の7要素の存在検査を追加し、要素を改名した場合に `check:app` がfailすることも確認した。再実行した `npm run check` は単体 `pass 29` / `fail 0`、E2E `48 passed` でgreen。あわせて実装ログの様式（着手前記述の残存、旧回帰基準の重複、テンプレート見出しの欠落）を是正し、設計書 §9 の受け入れ条件と設計書インデックスへ未実施項目の注記を入れ、Obsidianへ Phase 3・Phase 4 を反映した。

- 2026-07-29: 残課題だった設計書 §8.4-10 のLAN実機確認を実施した。`npm run build` ＋ `npm run preview` で `dist` を配信し、`http://<PCのLAN IP>:3038/` へ **iPhone / Safari** と **PCブラウザ** の2経路で接続して、(1) `ゲスト` の自動作成、(2) プレイヤー追加、(3) 選択時のHUD名反映、(4) 中級1試合完了後の結果画面の通算表示、(5) 戦績画面への1件記録と日時・スコア・最大ラリー・試合時間の表示、(6) コンソールエラーなし、の6項目を確認した。非Secure Contextで `crypto.randomUUID` が使えない環境でも、`createId()` のフォールバック（`crypto.getRandomValues` → `formatUuidV4`）で `addPlayer` と `recordMatch` の両採番経路が動作することを実機で確認できた。なおLAN の http ではService Workerが登録されない（Secure Context限定、例外は `localhost` のみ）ため、オフライン起動はこの経路の確認対象外とした。サーバーのアクセスログはファイルに残さない構成のため、本確認の記録は操作者の目視結果に依る。

**実装中に記録した設計調整**

実装中に設計書本文へ反映した変更は次の1件。いずれも設計書側が正本であり、本ログでは要点のみ記録する。

- **Service Worker の `fetch` を厳密 cache-first へ変更し、E2E の対象をbuild済み `dist` へ移した**（設計書 §4.8・§8.2）。当初設計は `CACHE_NAME` を `table-tennis-v3` へ上げるだけだったが、`dist` のサーバー停止再読込で、旧実装がキャッシュ済み資産にも並行ネットワーク要求を出しJS/CSSを起動できないケースを検出したため。あわせてE2Eの `webServer` をVite開発サーバーから `npm run build` ＋ `vite preview` へ変更し、既存 `tests/game-smoke.spec.js` のPWAシナリオを、Chromiumのオフライン再読込とWebKitのアプリシェルキャッシュ検査を含む内容へ強化した（設計書は当初「`game-smoke.spec.js` は変更しない」としていた）。シナリオ数は6のまま。

設計レビューの反映内容と未確定事項の確定内容は、[機能設計書 §変更履歴・§10](./機能設計書_プレイヤー戦績とラケット表示改修_2026-07-27.md) と [設計レビュー §5](./設計レビュー_プレイヤー戦績とラケット表示改修_2026-07-28.md) が正本のため、本ログでは再掲しない。実装単位（垂直スライス S1〜S7）は設計書 §6 を参照する。

**締めチェックリスト**

- 完了日: 2026-07-28
- 実装した内容: 腕を廃止した独立ラケット表示、プレイヤーCRUD、IndexedDBによる端末内戦績、通算・難易度別・直近10試合の集計、試合時間・最大ラリー、保存世代管理、障害時フォールバックを実装した。実装中にService Workerを厳密cache-firstへ変更した（上記「実装中に記録した設計調整」）。
- 反映コミット: `a7fa030`（Phase A）/ `fa2b12a`（Phase B）。設計確定は `ccb62cf`、公開結果の記録は `acbc378`。締め後の追補（`check-app.mjs` の要素検査と文書是正）は別コミット。
- 検証結果: 最終 `npm run check` pass。lint、厳格TypeScript、PWA契約、Vite build、`dist`契約、単体29件、3環境E2E 48件がgreen。ローカルChromeで2試合連続記録と最大ラリー分離、320×568、ラケット操作、オフライン再起動を確認。Actions run `30363050159`、deployment `5640592726`、HTTP/PWA資産、公開Chrome確認も完了した。**設計書 §8.4-10 のLAN実機確認（非HTTPSオリジンでのID採番）も 2026-07-29 に実施し、全6項目を確認した**（下記の作業メモ参照）。設計書 §8.3 の対応表は全行が実施済みになった。
- 残課題・既知の問題: Phase 3 から継続する3件のみ（サーブ表示高さ26と物理解起点24の2単位差、`physics.ts` の共有 `acceleration`、iOS Safari bfcache復帰時のAIサーブタイマー）。本機能のスコープ外として未修正。
- 次フェーズへの申し送り:
  - 回帰基準は**単体29件・E2E 48件**（Phase 3 時点の17件・18件は上書き済み）。`OpponentAi` の乱数消費順／判断契約も引き続き維持する。
  - `MatchResult` のスナップショット境界、`matchSeq` の世代管理、`StatsStore` のトランザクション境界、cache-first PWA再起動を壊さない。
  - E2Eはbuild済み `dist` のプレビューに対して実行する。Vite開発サーバーへ戻すと、Service Workerのオフライン検証が意味を失う。
  - ID採番や保存経路に手を入れるときは、非HTTPSオリジン（LAN IP）での実機確認を再実施する。`localhost` と公開HTTPSはどちらもSecure Contextのため、`crypto.randomUUID` のフォールバック経路を踏まない。
- 関連ドキュメント: [機能設計書](./機能設計書_プレイヤー戦績とラケット表示改修_2026-07-27.md) / [設計レビュー](./設計レビュー_プレイヤー戦績とラケット表示改修_2026-07-28.md) / [設計書インデックス](./設計書インデックス.md) / [README](../README.md)
- Obsidian更新: 済（`Projects/my-app2 プロジェクト一覧.md` の table-tennis 節へ Phase 3・Phase 4 を追記）
- ステータス更新: 完了（GitHub Pages公開済み・次フェーズ待ち）

---

### Phase 3: 第2期リファクタリング — 完了

- 完了日: 2026-07-28
- 実装した内容: `opponentOf` と `resolveMiss` に重複判定を集約し、`utils.ts` の `clamp` / `moveToward` と `PADDLE_LIMIT` / `PLAYER_AIM_SPAN` でプレイヤー・AIの移動処理を共通化した。ゲーム内の23定数を `config.ts` へ移し、相手AIの状態・移動・予測・ショット判断・サーブ選択を `OpponentAi` として `src/ai.ts` へ分離した。AIの決定論的単体テスト5件とAIサーブE2E 1シナリオを追加した。
- 反映コミット: `a08d25e`（R0基準文書）/ `b1a1f11`（R0設計修正）/ `3af8c1e`（R1）/ `fc225b6`（R2）/ `a8e05b9`（R3）/ `5fe10db`（R4）/ `03be229`（R5）/ `68e4fb5`（フェーズ締め）/ `0a8d5f8`（`master`統合）
- 検証結果: 最終 `npm run check` pass。ESLint、厳格TypeScript型検査、PWAソース契約検査、Vite build、`dist`契約検査がgreen。単体テスト17件、デスクトップChromium／iPhone相当WebKit／Pixel 7相当ChromiumのE2Eテスト18件がすべてpass。`game.ts` はR4時点1118行から1029行へ89行削減し、`ai.ts` は171行。ローカルChrome実操作では中級0–0、5種類のサーブ表示、横右サーブ、`RALLY 3`（2往復）、ポイント後1259msで次サーブ、合計2点後の相手サーブ表示、AI自動サーブ、一時停止／再開、コンソールエラー0件を確認した。公開時はGitHub Actions run `30356647250` のbuild（E2E 18件を含む）とdeployがsuccess。Pages deployment `5639277108` のSHAは統合コミット `0a8d5f8` と一致した。HTML、manifest、Service Worker、JS、CSS、アイコンはHTTP 200で、公開JS/CSSはローカルbuildとSHA-256一致。公開Chromeでタイトル、試合開始、中級0–0、サーブ5種類、横右サーブ、AI返球、AI自動サーブ、コンソールエラー0件を確認した。
- 実装中に記録した設計調整: 日本語パスのGitクォートを考慮した索引検査、R1〜R3の実測件数、R4旧値検査の曖昧な`1000`除外、明示列挙型`test:unit`へのAIテスト追加、厳格TypeScript向け`scriptedRandom`、Node.js 24 strip-only向け通常privateフィールド、R5行数条件を計画書へ反映した。いずれも対象挙動は変更していない。
- 残課題・既知の問題: (1) サーブ表示高さ26と物理解起点24の2単位差、(2) `physics.ts` の共有`acceleration`が将来並列化時に混線し得る点、(3) iOS Safari bfcache復帰時にAIサーブタイマーが再スケジュールされない可能性（未再現）。いずれも本リファクタリングの挙動不変スコープ外として未修正。
- 次フェーズへの申し送り: [機能設計書_プレイヤー戦績とラケット表示改修](./機能設計書_プレイヤー戦績とラケット表示改修_2026-07-27.md) に基づくv0.3.0実装へ進む。単体17件・E2E 18件と `OpponentAi` の乱数消費順／判断契約を回帰基準として維持し、上記既知課題を同機能へ無断で混在させない。
- 関連ドキュメント: [リファクタリング計画書](./リファクタリング計画書_2026-07-27.md) / [設計レビュー](./設計レビュー_リファクタリング計画_2026-07-27.md) / [機能設計書_プレイヤー戦績とラケット表示改修](./機能設計書_プレイヤー戦績とラケット表示改修_2026-07-27.md) / [設計書インデックス](./設計書インデックス.md) / [README](../README.md)
- ステータス更新: 完了（GitHub Pages公開済み・次フェーズ待ち）

### Phase 2: サーブ種類追加とゲーム基盤リファクタリング — 完了

- 完了日: 2026-07-27
- 実装した内容: v0.2.0として上回転・下回転・横左・横右・ナックルの5種類、スマートフォン優先の選択UI、難易度別AI選択を追加した。ゲーム処理をTypeScriptモジュールへ分割し、Pointer Eventsへ統一した。Vite固定名アセット、Service Worker `table-tennis-v2`、検査済み`dist/`を公開するbuild-gated Pagesワークフローへ移行した。
- 反映コミット: `e6628a3`
- 検証結果: `npm run check` pass。Lint、厳格TypeScript型検査、ソース／`dist`契約検査、Vite build、単体テスト9件、デスクトップChromium／iPhone相当WebKit／Pixel 7相当ChromiumのE2Eテスト15件がすべてpass。公開準備検査は `StaticReadiness: true`、`RiskyTrackedPaths: []`。GitHub Actions run `30258679962` のbuild/deployがpassし、deployment `5620990887` はコミット `e6628a3` でsuccess。公開URLのHTML、manifest、Service Worker、JavaScript、CSS、アイコンがHTTP 200。Chrome実操作で5種類表示、横右選択・サーブ実行・ラリー遷移・コンソールエラーなしを確認した。
- 残課題・既知の問題: なし。
- 次フェーズへの申し送り: サーブプロファイル値を変更するときは、5種類×両サーバー方向×代表3狙いの成立性テストを維持する。アセット追加時は固定名のVite出力、`sw.js`のアプリシェル、`scripts/check-dist.mjs`を同時更新する。PCキーボード／ゲームパッド入力は既存の入力通知インターフェースを利用する別設計とする。
- 関連ドキュメント: [実装設計書](./実装設計書_サーブ種類追加とゲーム基盤リファクタリング_2026-07-27.md) / [設計書インデックス](./設計書インデックス.md) / [README](../README.md)
- ステータス更新: 完了（次フェーズ待ち）

### Phase 1: ローカル開発・検証環境整備 — 完了

- 完了日: 2026-07-27
- 実装した内容: `D:\my-app2\sitar\table-tennis` にNode.js 24、Vite、ESLint、Node Test Runner、Playwright、PWA構成検査、GitHub Actionsを追加し、依存関係をロックした。開発ポート3038を固定した。既存ゲーム本体は変更していない。
- 反映コミット: `452ef87`
- 検証結果: `npm run check` pass。Lint pass、PWA構成検査 pass、Vite build pass、単体テスト3件 pass、デスクトップChromium／iPhone相当WebKitのE2Eテスト6件 pass。親ポート検査 pass。
- 残課題・既知の問題: GitHub Pagesは従来どおりソースの `table-tennis/` を直接配布しており、`dist/` 配布への切り替えは未実施。ゲーム本体のファイル分割・TypeScript化は次フェーズ以降の検討対象。
- 次フェーズへの申し送り: 機能変更や分割に着手する前に、現行E2Eを回帰基準として維持する。Service Workerのキャッシュ名更新と配布物整合を確認する。
- 関連ドキュメント: [README](../README.md)
- ステータス更新: 完了（次フェーズ待ち）
